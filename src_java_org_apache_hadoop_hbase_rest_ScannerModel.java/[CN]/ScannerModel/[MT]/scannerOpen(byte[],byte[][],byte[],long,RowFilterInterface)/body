{
  try {
    HTable table;
    table=new HTable(tableName);
    return new ScannerIdentifier(scannerMaster.addScanner(table.getScanner(columns,startRow,timestamp,filter)));
  }
 catch (  IOException e) {
    throw new HBaseRestException(e);
  }
}
