{
  while (!master.closed.get()) {
    if (master.rootScanned && master.numberOfMetaRegions.get() == master.onlineMetaRegions.size()) {
      break;
    }
    try {
      wait(master.threadWakeFrequency);
    }
 catch (    InterruptedException e) {
    }
  }
  return master.closed.get();
}
