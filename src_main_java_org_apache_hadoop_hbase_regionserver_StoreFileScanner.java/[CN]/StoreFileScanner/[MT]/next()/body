{
  KeyValue retKey=cur;
  try {
    if (cur != null) {
      hfs.next();
      cur=hfs.getKeyValue();
    }
  }
 catch (  IOException e) {
    throw new IOException("Could not iterate " + this,e);
  }
  return retKey;
}
