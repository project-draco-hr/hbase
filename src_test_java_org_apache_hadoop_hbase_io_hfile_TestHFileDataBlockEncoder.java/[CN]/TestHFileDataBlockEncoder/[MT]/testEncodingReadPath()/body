{
  HFileBlock origBlock=getSampleHFileBlock();
  HFileBlock afterDisk=blockEncoder.afterReadFromDisk(origBlock);
  blockEncoder.afterReadFromDiskAndPuttingInCache(afterDisk,false,includesMemstoreTS);
}
