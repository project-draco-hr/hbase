{
  try {
    doWriting();
  }
 catch (  IOException e) {
    flushToConsole(getName() + " Writer exiting " + e);
  }
catch (  InterruptedException e) {
    flushToConsole(getName() + " Writer exiting " + e);
  }
}
