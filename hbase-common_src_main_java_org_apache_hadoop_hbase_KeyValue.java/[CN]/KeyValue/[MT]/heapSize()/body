{
  int sum=0;
  sum+=ClassSize.OBJECT;
  sum+=2 * ClassSize.REFERENCE;
  sum+=2 * ClassSize.align(ClassSize.ARRAY);
  sum+=ClassSize.align(length);
  sum+=3 * Bytes.SIZEOF_INT;
  sum+=2 * Bytes.SIZEOF_LONG;
  return ClassSize.align(sum);
}
