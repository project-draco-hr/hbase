{
  if (!(other instanceof KeyValue)) {
    return false;
  }
  KeyValue kv=(KeyValue)other;
  return Bytes.equals(getBuffer(),getKeyOffset(),getKeyLength(),kv.getBuffer(),kv.getKeyOffset(),kv.getKeyLength());
}
