{
  while (iter.hasNext()) {
    KeyValue next=iter.next();
    int ret=KeyValue.COMPARATOR.compare(next,seekKv);
    if (ret >= 0) {
      current=next;
      return true;
    }
  }
  return false;
}
