{
  super(conf,fs,dirName,compression);
  this.dirName=dirName;
  this.fs=fs;
  if (filter) {
    this.bloomFilter=new BloomFilter((int)Math.ceil((DEFAULT_NUMBER_OF_HASH_FUNCTIONS * (1.0 * nrows)) / Math.log(2.0)),(int)DEFAULT_NUMBER_OF_HASH_FUNCTIONS);
  }
 else {
    this.bloomFilter=null;
  }
}
