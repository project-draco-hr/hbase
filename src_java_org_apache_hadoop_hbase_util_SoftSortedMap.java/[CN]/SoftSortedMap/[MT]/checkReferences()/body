{
  SoftValue<K,V> sv;
  Object obj;
  while ((obj=referenceQueue.poll()) != null) {
    if (LOG.isDebugEnabled()) {
      LOG.debug("Reference for key " + ((SoftValue<K,V>)obj).key.toString() + " has been cleared.");
    }
    internalMap.remove(((SoftValue<K,V>)obj).key);
  }
}
