{
  checkReferences();
  SoftValue<K,V> value=internalMap.get(key);
  if (value == null) {
    return null;
  }
 else {
    if (value.get() == null) {
      internalMap.remove(key);
      return null;
    }
 else {
      return value.get();
    }
  }
}
