{
  checkReferences();
  SoftValue<K,V> value=internalMap.get(key);
  if (value == null) {
    return null;
  }
  if (value.get() == null) {
    internalMap.remove(key);
    return null;
  }
  return value.get();
}
