{
  LOG.info("Starting shutdown thread");
  this.instance.stop();
  Threads.shutdown(this.mainThread);
  try {
    FileSystem.closeAll();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  LOG.info("Shutdown thread complete");
}
