{
  try {
    if (LOG.isDebugEnabled()) {
      LOG.debug("Closing region " + r.toString());
    }
    r.close();
  }
 catch (  IOException e) {
    LOG.error("Error closing region " + r.toString(),RemoteExceptionHandler.checkIOException(e));
  }
}
