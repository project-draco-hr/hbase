{
  EventType type=event.getType();
  LOG.info("Got ZooKeeper event, state: " + event.getState() + ", type: "+ type+ ", path: "+ event.getPath());
  if (type == EventType.NodeCreated) {
    getMaster();
  }
  watchMasterAddress();
}
