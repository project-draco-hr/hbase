{
  if (!fsOk) {
    return false;
  }
  if (!(leases.isAlive() && compactSplitThread.isAlive() && cacheFlusher.isAlive()&& logRoller.isAlive()&& workerThread.isAlive())) {
    stop();
    return false;
  }
  return true;
}
