{
  if (!fsOk) {
    return false;
  }
  if (!(leases.isAlive() && compactSplitThread.isAlive() && cacheFlusher.isAlive()&& hlogRoller.isAlive()&& workerThread.isAlive()&& this.majorCompactionChecker.isAlive())) {
    stop();
    return false;
  }
  return true;
}
