{
  checkOpen();
  requestCount.incrementAndGet();
  try {
    List<KeyValue> results=getRegion(regionName).get(row,column,timestamp,numVersions);
    return Cell.createSingleCellArray(results);
  }
 catch (  Throwable t) {
    throw convertThrowableToIOE(cleanup(t));
  }
}
