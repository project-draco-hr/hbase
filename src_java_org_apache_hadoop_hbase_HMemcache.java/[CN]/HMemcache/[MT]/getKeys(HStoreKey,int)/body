{
  this.lock.obtainReadLock();
  try {
    List<HStoreKey> results=getKeys(this.memcache,origin,versions);
    for (int i=history.size() - 1; i >= 0; i--) {
      results.addAll(results.size(),getKeys(history.elementAt(i),origin,versions == HConstants.ALL_VERSIONS ? versions : (results != null ? versions - results.size() : versions)));
    }
    return results;
  }
  finally {
    this.lock.releaseReadLock();
  }
}
