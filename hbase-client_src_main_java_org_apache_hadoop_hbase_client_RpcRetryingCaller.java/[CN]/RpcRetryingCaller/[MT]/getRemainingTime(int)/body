{
  if (callTimeout <= 0) {
    return 0;
  }
 else {
    int remainingTime=(int)(callTimeout - (EnvironmentEdgeManager.currentTimeMillis() - this.globalStartTime));
    if (remainingTime < MIN_RPC_TIMEOUT) {
      remainingTime=MIN_RPC_TIMEOUT;
    }
    return remainingTime;
  }
}
