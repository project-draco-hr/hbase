{
  int remaining=(int)(callTimeout - (EnvironmentEdgeManager.currentTimeMillis() - this.globalStartTime));
  if (remaining < MIN_RPC_TIMEOUT) {
    remaining=MIN_RPC_TIMEOUT;
  }
  RpcClient.setRpcTimeout(remaining);
}
