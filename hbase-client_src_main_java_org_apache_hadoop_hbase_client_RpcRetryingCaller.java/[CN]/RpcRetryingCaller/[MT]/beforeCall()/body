{
  if (callTimeout > 0) {
    remainingTime=(int)(callTimeout - (EnvironmentEdgeManager.currentTimeMillis() - this.globalStartTime));
    if (remainingTime < MIN_RPC_TIMEOUT) {
      remainingTime=MIN_RPC_TIMEOUT;
    }
  }
 else {
    remainingTime=0;
  }
}
