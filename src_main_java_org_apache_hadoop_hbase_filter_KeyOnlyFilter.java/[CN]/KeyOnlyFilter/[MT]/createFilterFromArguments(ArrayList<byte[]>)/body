{
  if (filterArguments.size() != 0) {
    throw new IllegalArgumentException("Incorrect Arguments passed to KeyOnlyFilter. " + "Expected: 0 but got: " + filterArguments.size());
  }
  return new KeyOnlyFilter();
}
