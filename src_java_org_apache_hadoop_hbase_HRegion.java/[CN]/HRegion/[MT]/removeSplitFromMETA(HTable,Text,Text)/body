{
  long lockid=t.startBatchUpdate(regionName);
  t.delete(lockid,split);
  t.commit(lockid);
  if (LOG.isDebugEnabled()) {
    LOG.debug("Removed " + split + " from "+ regionName+ " from table "+ t.getTableName());
  }
}
