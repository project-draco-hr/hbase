{
  if (updatesByColumn == null || updatesByColumn.size() <= 0) {
    return;
  }
  this.log.append(regionInfo.getRegionName(),regionInfo.getTableDesc().getName(),row,updatesByColumn,timestamp);
  this.memcache.add(row,updatesByColumn,timestamp);
}
