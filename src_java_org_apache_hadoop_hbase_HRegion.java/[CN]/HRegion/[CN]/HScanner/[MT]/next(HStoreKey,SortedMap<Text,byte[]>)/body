{
  boolean haveResults=false;
  for (int i=0; i < scanners.length; i++) {
    if (scanners[i] != null) {
      if (scanners[i].next(key,results)) {
        haveResults=true;
      }
 else {
        closeScanner(i);
      }
    }
  }
  return haveResults;
}
