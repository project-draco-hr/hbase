{
  ConsensusProvider cp=ConsensusProviderFactory.getConsensusProvider(conf);
  JVMClusterUtil.RegionServerThread rst=JVMClusterUtil.createRegionServerThread(config,cp,this.regionServerClass,index);
  this.regionThreads.add(rst);
  return rst;
}
