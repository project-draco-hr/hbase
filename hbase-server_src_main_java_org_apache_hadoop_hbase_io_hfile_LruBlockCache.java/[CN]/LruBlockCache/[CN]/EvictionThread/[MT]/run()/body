{
  enteringRun=true;
  while (this.go) {
synchronized (this) {
      try {
        this.wait(1000 * 10);
      }
 catch (      InterruptedException e) {
      }
    }
    LruBlockCache cache=this.cache.get();
    if (cache == null)     break;
    cache.evict();
  }
}
