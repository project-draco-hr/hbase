{
  super(EventType.C_M_MODIFY_TABLE,tableName,server,masterServices);
  this.htd=htd;
  if (!Bytes.equals(tableName,htd.getName())) {
    throw new IOException("TableDescriptor name & tableName must match: " + htd.getNameAsString() + " vs "+ Bytes.toString(tableName));
  }
}
