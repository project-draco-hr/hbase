{
  String archiveName=getConfiguredArchiveDirName(conf);
  Path root=tabledir.getParent();
  return archiveName.length() == 0 ? new Path(root,tabledir) : new Path(new Path(root,archiveName),tabledir.getName());
}
