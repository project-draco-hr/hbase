{
  for (  MasterEnvironment env : coprocessors) {
    if (env.getInstance() instanceof MasterObserver) {
      ((MasterObserver)env.getInstance()).postBalanceSwitch(env,oldValue,newValue);
      if (env.shouldComplete()) {
        break;
      }
    }
  }
}
