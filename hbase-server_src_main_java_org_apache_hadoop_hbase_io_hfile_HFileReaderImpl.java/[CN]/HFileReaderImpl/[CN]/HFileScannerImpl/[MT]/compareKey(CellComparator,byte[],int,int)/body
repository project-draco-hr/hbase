{
  KeyValue.KeyOnlyKeyValue keyOnlyKv=new KeyValue.KeyOnlyKeyValue(key,offset,length);
  return comparator.compare(keyOnlyKv,blockBuffer.array(),blockBuffer.arrayOffset() + blockBuffer.position() + KEY_VALUE_LEN_SIZE,currKeyLen);
}
