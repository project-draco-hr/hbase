{
  int nextKvPos=blockBuffer.position() + KEY_VALUE_LEN_SIZE + currKeyLen+ currValueLen+ currMemstoreTSLen;
  if (this.reader.getFileContext().isIncludesTags()) {
    nextKvPos+=Bytes.SIZEOF_SHORT + currTagsLen;
  }
  return nextKvPos;
}
