{
  this.lock.writeLock().lock();
  try {
    KeyValue viable=getNextViableRow();
    openScanner(viable.getRow());
    LOG.debug("Replaced Scanner Readers at row " + viable.getRow().toString());
  }
  finally {
    this.lock.writeLock().unlock();
  }
}
