{
  super();
  this.server=server;
  conf=server.conf;
  this.optionalLogRollInterval=conf.getLong("hbase.regionserver.optionallogrollinterval",30L * 60L * 1000L);
  lastLogRollTime=System.currentTimeMillis();
}
