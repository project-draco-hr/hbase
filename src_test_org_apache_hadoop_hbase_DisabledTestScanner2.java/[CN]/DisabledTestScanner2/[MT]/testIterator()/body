{
  HTable table=new HTable(this.conf,HConstants.ROOT_TABLE_NAME);
  Scanner scanner=table.getScanner(HConstants.COLUMN_FAMILY_ARRAY,HConstants.EMPTY_START_ROW);
  for (  RowResult e : scanner) {
    assertNotNull(e);
    assertNotNull(e.getRow());
  }
}
