{
  block=newBlock;
  if (block.getBlockType() != BlockType.DATA) {
    throw new IllegalStateException("ScannerV2 works only on data blocks");
  }
  blockBuffer=block.getBufferWithoutHeader();
  readKeyValueLen();
  blockFetches++;
}
