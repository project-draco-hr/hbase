{
  HFileBlock seekToBlock=((HFileReaderV2)reader).getDataBlockIndexReader().seekToDataBlock(key,offset,length,block);
  if (seekToBlock == null) {
    return -1;
  }
  return loadBlockAndSeekToKey(seekToBlock,rewind,key,offset,length,false);
}
