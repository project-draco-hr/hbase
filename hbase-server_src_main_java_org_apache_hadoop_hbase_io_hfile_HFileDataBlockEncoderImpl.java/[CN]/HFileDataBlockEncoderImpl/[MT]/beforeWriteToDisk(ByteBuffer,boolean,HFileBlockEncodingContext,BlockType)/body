{
  if (onDisk == DataBlockEncoding.NONE) {
    ((HFileBlockDefaultEncodingContext)encodeCtx).compressAfterEncoding(in.array(),blockType);
    return;
  }
  encodeBufferToHFileBlockBuffer(in,onDisk,includesMemstoreTS,encodeCtx);
}
