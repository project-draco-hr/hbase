{
  HFileContext newMeta=meta.clone();
  return (inCache != DataBlockEncoding.NONE) ? this.inCache.getEncoder().newDataBlockEncodingContext(this.inCache,dummyHeader,newMeta) : new HFileBlockDefaultEncodingContext(this.inCache,dummyHeader,newMeta);
}
