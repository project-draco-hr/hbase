{
  HFileContext newContext=new HFileContext(fileContext);
  return (inCache != DataBlockEncoding.NONE) ? this.inCache.getEncoder().newDataBlockEncodingContext(this.inCache,dummyHeader,newContext) : new HFileBlockDefaultEncodingContext(this.inCache,dummyHeader,newContext);
}
