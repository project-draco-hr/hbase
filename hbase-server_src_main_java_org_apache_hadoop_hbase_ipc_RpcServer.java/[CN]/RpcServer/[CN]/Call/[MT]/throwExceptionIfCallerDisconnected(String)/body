{
  if (!connection.channel.isOpen()) {
    long afterTime=System.currentTimeMillis() - timestamp;
    throw new CallerDisconnectedException("Aborting on region " + regionName + ", call "+ this+ " after "+ afterTime+ " ms, since "+ "caller disconnected");
  }
}
