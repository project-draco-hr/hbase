{
  return Threads.getBoundedCachedThreadPool(maxThreads,maxThreads * 3,TimeUnit.SECONDS,new ThreadFactory(){
    private int count=1;
    @Override public Thread newThread(    Runnable r){
      return new Thread(r,"CompactedfilesArchiver-" + count++);
    }
  }
);
}
