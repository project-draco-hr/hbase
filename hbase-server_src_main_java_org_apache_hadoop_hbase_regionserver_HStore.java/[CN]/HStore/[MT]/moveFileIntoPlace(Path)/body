{
  validateStoreFile(newFile);
  Path destPath=fs.commitStoreFile(getColumnFamilyName(),newFile);
  StoreFile result=new StoreFile(this.getFileSystem(),destPath,this.conf,this.cacheConf,this.family.getBloomFilterType(),this.dataBlockEncoder);
  result.createReader();
  return result;
}
