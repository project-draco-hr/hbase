{
  if (this.table == null || !this.table.getTableName().toString().equals(tableName)) {
    if (this.table != null) {
      this.table.close();
    }
    this.table=new HTable(this.conf,new Text(tableName));
  }
}
