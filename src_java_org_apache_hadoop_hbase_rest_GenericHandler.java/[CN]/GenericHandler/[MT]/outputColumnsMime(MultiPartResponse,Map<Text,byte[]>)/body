{
  for (  Map.Entry<Text,byte[]> e : m.entrySet()) {
    mpr.startPart("application/octet-stream",new String[]{"Content-Description: " + e.getKey().toString(),"Content-Transfer-Encoding: binary","Content-Length: " + e.getValue().length});
    mpr.getOut().write(e.getValue());
  }
}
