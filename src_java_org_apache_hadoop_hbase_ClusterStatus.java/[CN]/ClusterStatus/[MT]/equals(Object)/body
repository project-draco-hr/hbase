{
  if (this == o) {
    return true;
  }
  if (!(o instanceof ClusterStatus)) {
    return false;
  }
  return (getVersion() == ((ClusterStatus)o).getVersion()) && liveServerInfo.equals(((ClusterStatus)o).liveServerInfo) && deadServers.equals(((ClusterStatus)o).deadServers);
}
