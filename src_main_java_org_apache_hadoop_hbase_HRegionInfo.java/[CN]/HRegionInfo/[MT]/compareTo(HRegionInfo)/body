{
  if (o == null) {
    return 1;
  }
  int result=Bytes.compareTo(this.tableName,o.tableName);
  if (result != 0) {
    return result;
  }
  result=Bytes.compareTo(this.startKey,o.startKey);
  if (result != 0) {
    return result;
  }
  result=Bytes.compareTo(this.endKey,o.endKey);
  if (result != 0) {
    if (this.getEndKey().length == 0)     return 1;
    if (o.getEndKey().length == 0)     return -1;
    return result;
  }
  if (this.offLine == o.offLine)   return 0;
  if (this.offLine == true)   return -1;
  return 1;
}
