{
  OpenRegionRequest request=RequestConverter.buildOpenRegionRequest(regions);
  try {
    return admin.openRegion(null,request);
  }
 catch (  ServiceException se) {
    throw getRemoteException(se);
  }
}
