{
  TimerTask tt=new TimerTask(){
    @Override public void run(){
      closed=true;
synchronized (msgQueue) {
        msgQueue.clear();
        msgQueue.notifyAll();
      }
    }
  }
;
  Timer t=new Timer("Shutdown");
  t.schedule(tt,10);
}
