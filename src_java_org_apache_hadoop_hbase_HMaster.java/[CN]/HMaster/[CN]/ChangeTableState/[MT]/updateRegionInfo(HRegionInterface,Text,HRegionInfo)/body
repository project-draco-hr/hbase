{
  i.offLine=!online;
  ByteArrayOutputStream byteValue=new ByteArrayOutputStream();
  DataOutputStream s=new DataOutputStream(byteValue);
  i.write(s);
  srvr.put(regionName,clientId,lockid,COL_REGIONINFO,byteValue.toByteArray());
}
