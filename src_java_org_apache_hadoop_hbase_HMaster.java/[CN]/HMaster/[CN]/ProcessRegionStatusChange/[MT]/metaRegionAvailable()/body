{
  boolean available=true;
  if (isMetaTable) {
    if (!rootAvailable()) {
      available=false;
    }
  }
 else {
    if (!rootScanned || !metaTableAvailable()) {
      requeue();
      available=false;
    }
  }
  return available;
}
