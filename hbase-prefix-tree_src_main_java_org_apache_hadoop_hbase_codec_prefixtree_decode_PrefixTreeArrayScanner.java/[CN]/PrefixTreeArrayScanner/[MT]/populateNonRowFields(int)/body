{
  currentCellIndex=cellIndex;
  populateFamily();
  populateQualifier();
  if (blockMeta.getNumTagsBytes() != 0) {
    populateTag();
  }
  populateTimestamp();
  populateMvccVersion();
  populateType();
  populateValueOffsets();
}
