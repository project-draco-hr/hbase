{
  String randomDir=user.getShortName() + "__" + Bytes.toString(tableName)+ "__"+ (new BigInteger(RANDOM_WIDTH,random).toString(RANDOM_RADIX));
  return createStagingDir(baseDir,user,tableName,randomDir);
}
