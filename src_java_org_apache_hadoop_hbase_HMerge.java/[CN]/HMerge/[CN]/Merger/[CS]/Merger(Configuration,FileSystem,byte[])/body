{
  this.conf=conf;
  this.fs=fs;
  this.maxFilesize=conf.getLong("hbase.hregion.max.filesize",DEFAULT_MAX_FILE_SIZE);
  this.tabledir=new Path(fs.makeQualified(new Path(conf.get(HBASE_DIR))),Bytes.toString(tableName));
  Path logdir=new Path(tabledir,"merge_" + System.currentTimeMillis() + HREGION_LOGDIR_NAME);
  Path oldLogDir=new Path(tabledir,HREGION_OLDLOGDIR_NAME);
  this.hlog=new HLog(fs,logdir,oldLogDir,conf,null);
}
