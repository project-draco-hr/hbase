{
  compactingSetUp();
  Configuration conf=HBaseConfiguration.create();
  conf.setLong("hbase.hregion.compacting.memstore.type",2);
  this.memstore=new CompactingMemStore(conf,CellComparator.COMPARATOR,store,regionServicesForStores);
}
