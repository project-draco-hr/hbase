{
  LongTermArchivingHFileCleaner cleaner=(LongTermArchivingHFileCleaner)UTIL.getMiniHBaseCluster().getMaster().getHFileCleaner().getCleanerChain().get(1);
  while (cleaner.isArchiving(STRING_TABLE_NAME) == wasArchiving) {
    Thread.sleep(WAIT_FOR_ZK_ARCHIVE_STATE_CHANGE);
  }
}
