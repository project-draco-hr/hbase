{
  int tries=0;
  Exception last=null;
  while (tries++ <= maxTries) {
    try {
      UTIL.loadRegion(region,TEST_FAM);
      compactRegion(region,TEST_FAM);
      return;
    }
 catch (    Exception e) {
      last=e;
    }
  }
  throw last;
}
