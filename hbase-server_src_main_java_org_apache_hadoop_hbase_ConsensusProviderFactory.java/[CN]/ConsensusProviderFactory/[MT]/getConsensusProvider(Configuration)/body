{
  Class<? extends ConsensusProvider> consensusKlass=conf.getClass(HConstants.HBASE_CONSENSUS_PROVIDER_CLASS,ZkConsensusProvider.class,ConsensusProvider.class);
  return ReflectionUtils.newInstance(consensusKlass,conf);
}
