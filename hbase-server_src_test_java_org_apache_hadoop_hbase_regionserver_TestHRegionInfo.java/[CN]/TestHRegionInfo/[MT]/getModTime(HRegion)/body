{
  FileStatus[] statuses=r.getFilesystem().listStatus(new Path(r.getRegionDir(),HRegion.REGIONINFO_FILE));
  assertTrue(statuses != null && statuses.length == 1);
  return statuses[0].getModificationTime();
}
