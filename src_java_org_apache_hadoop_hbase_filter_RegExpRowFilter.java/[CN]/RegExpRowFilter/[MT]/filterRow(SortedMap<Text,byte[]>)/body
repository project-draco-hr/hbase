{
  for (  Entry<Text,byte[]> col : columns.entrySet()) {
    if (nullColumns.contains(col.getKey()) && !HLogEdit.isDeleted(col.getValue())) {
      return true;
    }
  }
  for (  Text col : equalsMap.keySet()) {
    if (!columns.containsKey(col)) {
      return true;
    }
  }
  return false;
}
