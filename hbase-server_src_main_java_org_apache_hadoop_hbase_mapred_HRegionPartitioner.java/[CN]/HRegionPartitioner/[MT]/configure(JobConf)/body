{
  try {
    this.table=new HTable(HBaseConfiguration.create(job),TableName.valueOf(job.get(TableOutputFormat.OUTPUT_TABLE)));
  }
 catch (  IOException e) {
    LOG.error(e);
  }
  try {
    this.startKeys=this.table.getStartKeys();
  }
 catch (  IOException e) {
    LOG.error(e);
  }
}
