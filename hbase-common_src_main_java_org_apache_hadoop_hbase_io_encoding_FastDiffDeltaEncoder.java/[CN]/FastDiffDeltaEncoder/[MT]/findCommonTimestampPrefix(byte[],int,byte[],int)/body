{
  int commonPrefix=0;
  while (commonPrefix < (KeyValue.TIMESTAMP_SIZE - 1) && curKvBuf[curKvTsOff + commonPrefix] == preKvBuf[preKvTsOff + commonPrefix]) {
    commonPrefix++;
  }
  return commonPrefix;
}
