{
  try {
    this.zkTable.setEnabledTable(tableName);
  }
 catch (  KeeperException e) {
    String errorMsg="Unable to ensure that the table " + tableName + " is enabled because of a ZooKeeper issue:"+ e;
    LOG.error(errorMsg);
    this.master.abort(errorMsg,e);
  }
}
