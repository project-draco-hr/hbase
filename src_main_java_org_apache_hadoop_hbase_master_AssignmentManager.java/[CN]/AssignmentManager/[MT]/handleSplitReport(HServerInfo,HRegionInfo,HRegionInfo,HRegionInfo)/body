{
  regionOffline(parent);
  regionOnline(a,hsi);
  regionOnline(b,hsi);
  if (this.zkTable.isDisablingOrDisabledTable(parent.getTableDesc().getNameAsString())) {
    unassign(a);
    unassign(b);
  }
}
