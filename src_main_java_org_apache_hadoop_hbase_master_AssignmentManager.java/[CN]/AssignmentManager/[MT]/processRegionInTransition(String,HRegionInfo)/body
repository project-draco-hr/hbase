{
  RegionTransitionData data=ZKAssign.getData(watcher,encodedRegionName);
  if (data == null)   return false;
  HRegionInfo hri=(regionInfo != null) ? regionInfo : MetaReader.getRegion(catalogTracker,data.getRegionName()).getFirst();
  processRegionsInTransition(data,hri);
  return true;
}
