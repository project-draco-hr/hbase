{
  HRegionInfo hri=findHRegionInfo(serverName,encodedName);
  if (hri == null) {
    LOG.warn("Region " + encodedName + " not found on server "+ serverName+ "; failed processing");
    return null;
  }
  return addToRegionsInTransition(hri);
}
