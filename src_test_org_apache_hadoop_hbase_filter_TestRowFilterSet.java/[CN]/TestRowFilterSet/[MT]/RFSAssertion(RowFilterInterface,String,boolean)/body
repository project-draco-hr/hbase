{
  Text testText=new Text(toTest);
  boolean filtered=filter.filter(testText);
  assertTrue("Filtering on '" + toTest + "'",assertTrue ? filtered : !filtered);
  filter.rowProcessed(filtered,testText);
}
