{
  for (long id=this.logSeqNum.get(); id < newvalue && !this.logSeqNum.compareAndSet(id,newvalue); id=this.logSeqNum.get()) {
    LOG.debug("Changed sequenceid from " + logSeqNum + " to "+ newvalue);
  }
}
