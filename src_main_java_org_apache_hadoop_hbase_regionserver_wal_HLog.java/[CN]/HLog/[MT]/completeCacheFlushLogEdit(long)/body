{
  byte[] data=Bytes.add(COMPLETE_CACHE_FLUSH,":".getBytes(Charsets.UTF_8),Bytes.toBytes(String.valueOf(seqIdOfFlush)));
  KeyValue kv=new KeyValue(METAROW,METAFAMILY,null,System.currentTimeMillis(),data);
  WALEdit e=new WALEdit();
  e.add(kv);
  return e;
}
