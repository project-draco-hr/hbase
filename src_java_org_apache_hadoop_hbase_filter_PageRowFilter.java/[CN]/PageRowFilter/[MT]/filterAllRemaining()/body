{
  boolean result=this.rowsAccepted > this.pageSize;
  if (LOG.isDebugEnabled()) {
    LOG.debug("filtering decision is " + result + " with rowsAccepted: "+ this.rowsAccepted);
  }
  return result;
}
