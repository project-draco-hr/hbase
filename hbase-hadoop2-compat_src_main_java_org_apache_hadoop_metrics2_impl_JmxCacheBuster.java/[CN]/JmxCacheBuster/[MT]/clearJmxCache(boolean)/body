{
  ScheduledFuture future=fut.get();
  if (!force && (future == null || (!future.isDone() && future.getDelay(TimeUnit.MILLISECONDS) > 100))) {
    return;
  }
  future=executor.getExecutor().schedule(new JmxCacheBusterRunnable(),5,TimeUnit.SECONDS);
  fut.set(future);
}
