{
  if (!hasCallId()) {
    return false;
  }
  if (!hasError()) {
    return false;
  }
  if (hasException()) {
    if (!getException().isInitialized()) {
      return false;
    }
  }
  return true;
}
