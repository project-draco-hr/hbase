{
  Category category=c.getAnnotation(Category.class);
  if (category != null) {
    if (xface == null) {
      return true;
    }
    for (    Class<?> cc : category.value()) {
      if (cc.equals(xface)) {
        return true;
      }
    }
  }
  return false;
}
