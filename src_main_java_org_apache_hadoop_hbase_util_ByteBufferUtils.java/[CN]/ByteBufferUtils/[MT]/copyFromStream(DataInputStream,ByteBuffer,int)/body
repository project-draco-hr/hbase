{
  if (buffer.hasArray()) {
    source.readFully(buffer.array(),buffer.position() + buffer.arrayOffset(),length);
    skip(buffer,length);
  }
 else {
    for (int i=0; i < length; ++i) {
      buffer.put(source.readByte());
    }
  }
}
