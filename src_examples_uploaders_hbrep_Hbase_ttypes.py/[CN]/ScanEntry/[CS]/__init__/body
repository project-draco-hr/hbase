def __init__(self, d=None):
    self.row = None
    self.columns = None
    if isinstance(d, dict):
        if ('row' in d):
            self.row = d['row']
        if ('columns' in d):
            self.columns = d['columns']
