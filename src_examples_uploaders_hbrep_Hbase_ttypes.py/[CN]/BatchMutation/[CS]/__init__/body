def __init__(self, d=None):
    self.row = None
    self.mutations = None
    if isinstance(d, dict):
        if ('row' in d):
            self.row = d['row']
        if ('mutations' in d):
            self.mutations = d['mutations']
