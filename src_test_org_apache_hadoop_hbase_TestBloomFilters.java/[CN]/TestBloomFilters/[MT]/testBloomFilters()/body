{
  try {
    Thread.sleep(conf.getLong(HConstants.THREAD_WAKE_FREQUENCY,10 * 1000) * 2);
  }
 catch (  InterruptedException e) {
  }
  try {
    for (int i=0; i < testKeys.length; i++) {
      byte[] value=table.get(testKeys[i],CONTENTS);
      if (value != null && value.length != 0) {
        System.err.println("non existant key: " + testKeys[i] + " returned value: "+ new String(value,HConstants.UTF8_ENCODING));
      }
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail();
  }
}
