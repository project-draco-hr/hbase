{
  super();
  conf.set("hbase.hregion.maxunflushed","90");
  conf.set("hbase.regionserver.maxlogentries","90");
  Logger.getLogger(HRegion.class).setLevel(Level.DEBUG);
  Logger.getLogger(HStore.class).setLevel(Level.DEBUG);
}
