{
  String userAndHost=sshUserName.isEmpty() ? hostname : (sshUserName + "@" + hostname);
  return new String[]{"bash","-c",StringUtils.join(new String[]{sshCmd,(sshOptions == null) ? "" : sshOptions,userAndHost,"\"" + sshBeforeCommand + StringUtils.join(super.getExecString()," ")+ "\""}," ")};
}
