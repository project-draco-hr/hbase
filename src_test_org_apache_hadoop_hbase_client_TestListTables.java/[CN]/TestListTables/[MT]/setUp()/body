{
  super.setUp();
  admin=new HBaseAdmin(conf);
  HColumnDescriptor family=new HColumnDescriptor(HConstants.COLUMN_FAMILY_STR);
  for (int i=0; i < tables.length; i++) {
    tables[i].addFamily(family);
    admin.createTable(tables[i]);
  }
}
