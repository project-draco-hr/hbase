{
  String value=this.zookeeperWrapper.getDataAndWatch(this.replicationZNode,this.replicationStateNodeName,this);
  if (value != null) {
    isReplicating.set(value.equals("true"));
    LOG.info("Replication is now " + (isReplicating.get() ? "started" : "stopped"));
  }
}
