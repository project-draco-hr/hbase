{
  final WALProvider temp=factory.getProvider(DELEGATE_PROVIDER,DEFAULT_DELEGATE_PROVIDER,listeners,providerId + "-" + UUID.randomUUID());
  final WALProvider extant=cached.putIfAbsent(group,temp);
  if (null != extant) {
    temp.close();
    return extant;
  }
  return temp;
}
