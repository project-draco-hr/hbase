{
  WAL log=cached.get(group);
  if (null == log) {
synchronized (this.walCacheLock) {
      log=cached.get(group);
      if (null == log) {
        log=populateCache(group);
      }
    }
  }
  return log;
}
