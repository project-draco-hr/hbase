{
  boolean exists=true;
  try {
    SortedMap<Text,HRegionLocation> servers=getTableServers(tableName);
    if (servers == null || servers.size() == 0) {
      exists=false;
    }
  }
 catch (  IOException e) {
    exists=false;
  }
  return exists;
}
