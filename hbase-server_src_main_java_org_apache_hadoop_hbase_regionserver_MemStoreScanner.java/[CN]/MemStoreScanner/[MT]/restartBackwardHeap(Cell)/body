{
  boolean res=false;
  for (  SegmentScanner scan : scanners) {
    res|=scan.seekToPreviousRow(cell);
  }
  this.backwardHeap=new ReversedKeyValueHeap(scanners,backwardReferenceToMemStore.getComparator());
  return res;
}
