{
  KeyValueHeap heap=(Type.USER_SCAN_BACKWARD == type) ? backwardHeap : forwardHeap;
  for (Cell currentCell=heap.next(); currentCell != null; currentCell=heap.next()) {
    return currentCell;
  }
  return null;
}
