{
  while (t.isAlive()) {
    try {
      if (joinwait == -1) {
        t.join();
      }
 else {
        t.join(joinwait);
      }
    }
 catch (    InterruptedException e) {
      LOG.warn(t.getName() + "; joinwait=" + joinwait,e);
    }
  }
}
