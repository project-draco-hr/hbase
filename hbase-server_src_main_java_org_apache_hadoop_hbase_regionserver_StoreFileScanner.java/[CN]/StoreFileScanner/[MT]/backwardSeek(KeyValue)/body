{
  seek(key);
  if (cur == null || Bytes.compareTo(cur.getBuffer(),cur.getRowOffset(),cur.getRowLength(),key.getBuffer(),key.getRowOffset(),key.getRowLength()) > 0) {
    return seekToPreviousRow(key);
  }
  return true;
}
