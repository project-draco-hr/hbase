{
  int result=s.reseekTo(k.getBuffer(),k.getKeyOffset(),k.getKeyLength());
  if (result <= 0) {
    if (!s.isSeeked()) {
      return s.seekTo();
    }
    return true;
  }
 else {
    return s.next();
  }
}
