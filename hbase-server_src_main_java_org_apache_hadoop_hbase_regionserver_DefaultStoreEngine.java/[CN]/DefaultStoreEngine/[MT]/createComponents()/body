{
  storeFileManager=new DefaultStoreFileManager(this.comparator,this.conf);
  compactor=new DefaultCompactor(this.conf,this.store);
  compactionPolicy=new DefaultCompactionPolicy(this.conf,this.store);
}
