{
  if (!scanMetricsPublished)   writeScanMetrics();
  if (callable != null) {
    callable.setClose();
    try {
      callable.withRetries();
    }
 catch (    IOException e) {
    }
    callable=null;
  }
  closed=true;
}
