{
  try {
    if (LOG.isDebugEnabled()) {
      LOG.debug("Remove log: " + log);
    }
    log.removeFile();
    logs.remove(log);
  }
 catch (  IOException e) {
    LOG.error("Unable to remove log: " + log,e);
    return false;
  }
  return true;
}
