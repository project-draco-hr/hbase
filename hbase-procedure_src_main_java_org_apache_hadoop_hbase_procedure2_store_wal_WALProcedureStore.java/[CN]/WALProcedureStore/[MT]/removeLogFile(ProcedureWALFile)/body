{
  try {
    LOG.debug("remove log: " + log);
    log.removeFile();
    logs.remove(log);
  }
 catch (  IOException e) {
    LOG.error("unable to remove log " + log,e);
    return false;
  }
  return true;
}
