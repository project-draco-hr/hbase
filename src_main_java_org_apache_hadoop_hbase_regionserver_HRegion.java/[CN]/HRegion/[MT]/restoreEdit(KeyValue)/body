{
  Map<byte[],List<KeyValue>> map=new TreeMap<byte[],List<KeyValue>>(Bytes.BYTES_COMPARATOR);
  map.put(kv.getFamily(),Collections.singletonList(kv));
  if (kv.isDelete()) {
    delete(map,true);
  }
 else {
    put(map,true);
  }
}
