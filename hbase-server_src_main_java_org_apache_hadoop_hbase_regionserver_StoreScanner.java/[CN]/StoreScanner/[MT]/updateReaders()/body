{
  lock.lock();
  try {
    if (this.closing)     return;
    if (this.heap == null)     return;
    this.lastTop=this.peek();
    this.heap.close();
    this.heap=null;
  }
  finally {
    lock.unlock();
  }
}
