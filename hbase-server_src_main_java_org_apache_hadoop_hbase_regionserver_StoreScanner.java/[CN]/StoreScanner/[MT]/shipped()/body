{
  lock.lock();
  try {
    for (    KeyValueHeap h : this.heapsForDelayedClose) {
      h.close();
    }
    this.heapsForDelayedClose.clear();
    if (this.heap != null) {
      this.heap.shipped();
    }
  }
  finally {
    lock.unlock();
  }
}
