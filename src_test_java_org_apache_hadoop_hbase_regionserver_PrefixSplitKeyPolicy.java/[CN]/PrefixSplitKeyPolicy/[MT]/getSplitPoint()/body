{
  byte[] splitPoint=super.getSplitPoint();
  if (splitPoint != null && splitPoint.length > 0) {
    return Arrays.copyOf(splitPoint,Math.min(prefix_length,splitPoint.length));
  }
 else {
    return splitPoint;
  }
}
