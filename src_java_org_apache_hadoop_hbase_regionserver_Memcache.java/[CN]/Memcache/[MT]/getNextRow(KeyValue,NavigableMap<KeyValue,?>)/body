{
  KeyValue result=null;
  SortedMap<KeyValue,?> tail=kv == null ? map : map.tailMap(kv);
  for (  Map.Entry<KeyValue,?> i : tail.entrySet()) {
    if (comparator.compareRows(i.getKey(),kv) <= 0)     continue;
    result=i.getKey();
    break;
  }
  return result;
}
