{
  KeyValue result=null;
  SortedSet<KeyValue> tailset=kv == null ? set : set.tailSet(kv);
  for (  KeyValue i : tailset) {
    if (comparator.compareRows(i,kv) <= 0)     continue;
    result=i;
    break;
  }
  return result;
}
