{
  SortedSet<KeyValue> s=set.tailSet(kv);
  if (s.isEmpty()) {
    return false;
  }
  boolean removed=false;
  for (  KeyValue k : s) {
    if (this.comparatorIgnoreType.compare(k,kv) == 0) {
      s.remove(k);
      removed=true;
      continue;
    }
    break;
  }
  return removed;
}
