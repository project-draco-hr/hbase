{
  LOG.debug("Stopping catalog tracker " + this.connection.toString() + "; will interrupt blocked waits on root and meta");
  this.stopped=true;
  this.rootRegionTracker.stop();
  this.metaNodeTracker.stop();
synchronized (this.metaAvailable) {
    this.metaAvailable.notifyAll();
  }
}
