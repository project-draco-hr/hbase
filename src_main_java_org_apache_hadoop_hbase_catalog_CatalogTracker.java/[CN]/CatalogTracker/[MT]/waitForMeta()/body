{
  while (!this.stopped) {
    try {
      if (waitForMeta(100) != null)       break;
    }
 catch (    NotAllMetaRegionsOnlineException e) {
      LOG.info("Retrying",e);
    }
catch (    IOException e) {
      LOG.info("Retrying",e);
    }
  }
}
