{
  HRegionInterface connection=null;
  try {
    connection=waitForRootServerConnection(timeout);
  }
 catch (  NotAllMetaRegionsOnlineException e) {
  }
catch (  IOException e) {
    throw e;
  }
  return (connection == null) ? false : verifyRegionLocation(connection,HRegionInfo.ROOT_REGIONINFO.getRegionName());
}
