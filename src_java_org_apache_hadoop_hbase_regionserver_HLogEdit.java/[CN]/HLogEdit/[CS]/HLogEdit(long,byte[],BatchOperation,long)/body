{
  this(new KeyValue(row,op.getColumn(),timestamp,op.isPut() ? KeyValue.Type.Put : KeyValue.Type.Delete,op.getValue()));
  this.transactionId=transactionId;
  this.operation=TransactionalOperation.WRITE;
  this.isTransactionEntry=true;
}
