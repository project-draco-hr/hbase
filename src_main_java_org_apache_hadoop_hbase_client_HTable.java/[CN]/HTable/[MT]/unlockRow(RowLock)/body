{
  new ServerCallable<Void>(connection,tableName,rl.getRow(),operationTimeout){
    public Void call() throws IOException {
      server.unlockRow(location.getRegionInfo().getRegionName(),rl.getLockId());
      return null;
    }
  }
.withRetries();
}
