{
  if (this.closed) {
    return;
  }
  flushCommits();
  if (cleanupOnClose) {
    this.pool.shutdown();
    if (this.connection != null) {
      this.connection.close();
    }
  }
  this.closed=true;
}
