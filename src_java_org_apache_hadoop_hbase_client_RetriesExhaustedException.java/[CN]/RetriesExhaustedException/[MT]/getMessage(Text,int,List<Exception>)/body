{
  String buffer="Trying to contact region server for row '" + row + "', but failed after "+ (numTries + 1)+ " attempts.\nExceptions:\n";
  for (  Exception e : exceptions) {
    buffer+=e.toString() + "\n";
  }
  return buffer;
}
