{
  HRegionServer server=getRegionServer(serverNumber);
  try {
    LOG.info("Aborting " + server.getHServerInfo().toString());
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  server.abort();
}
