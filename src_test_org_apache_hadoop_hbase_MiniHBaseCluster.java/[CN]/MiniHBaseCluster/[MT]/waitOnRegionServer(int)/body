{
  if (serverNumber >= regionServers.size()) {
    throw new ArrayIndexOutOfBoundsException("serverNumber > number of region servers");
  }
  try {
    this.regionThreads.get(serverNumber).join();
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
}
