{
  try {
synchronized (this) {
      if (isRunning) {
        master=new HMaster(conf);
      }
      isInitialized=true;
    }
  }
 catch (  Throwable e) {
    shutdown();
    System.err.println("HMaster crashed:");
    e.printStackTrace();
synchronized (this) {
      isCrashed=true;
    }
  }
}
