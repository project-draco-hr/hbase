{
  isRunning=false;
  if (master != null) {
    try {
      master.stop();
    }
 catch (    IOException e) {
      System.err.println("Master crashed during stop");
      e.printStackTrace();
    }
 finally {
      master.join();
      master=null;
    }
  }
}
