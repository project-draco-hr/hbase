{
  try {
synchronized (this) {
      if (isRunning) {
        server=new HRegionServer(conf);
      }
    }
    server.run();
  }
 catch (  Throwable e) {
    shutdown();
    System.err.println("HRegionServer crashed:");
    e.printStackTrace();
  }
}
