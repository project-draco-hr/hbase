{
  isRunning=false;
  if (server != null) {
    try {
      server.stop();
    }
 catch (    IOException e) {
      System.err.println("HRegionServer crashed during stop");
      e.printStackTrace();
    }
 finally {
      server.join();
      server=null;
    }
  }
}
