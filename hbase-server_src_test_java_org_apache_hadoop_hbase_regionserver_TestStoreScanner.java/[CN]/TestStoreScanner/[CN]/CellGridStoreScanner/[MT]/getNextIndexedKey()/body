{
  return count.get() > CELL_GRID_BLOCK4_BOUNDARY ? CellUtil.createFirstOnRow(CELL_GRID[CELL_GRID_BLOCK5_BOUNDARY]) : count.get() > CELL_GRID_BLOCK3_BOUNDARY ? CellUtil.createFirstOnRow(CELL_GRID[CELL_GRID_BLOCK4_BOUNDARY]) : count.get() > CELL_GRID_BLOCK2_BOUNDARY ? CellUtil.createFirstOnRow(CELL_GRID[CELL_GRID_BLOCK3_BOUNDARY]) : CellUtil.createFirstOnRow(CELL_GRID[CELL_GRID_BLOCK2_BOUNDARY]);
}
