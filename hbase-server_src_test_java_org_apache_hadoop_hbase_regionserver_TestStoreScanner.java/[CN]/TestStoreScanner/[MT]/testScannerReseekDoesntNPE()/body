{
  List<KeyValueScanner> scanners=scanFixture(kvs);
  StoreScanner scan=new StoreScanner(new Scan(),scanInfo,scanType,getCols("a","d"),scanners);
  scan.updateReaders();
  scan.updateReaders();
  scan.peek();
}
