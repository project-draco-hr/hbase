{
  HFileBlock cacheFormatBlock=fsBlockWriter.getBlockForCaching();
  passSchemaMetricsTo(cacheFormatBlock);
  cacheConf.getBlockCache().cacheBlock(HFile.getBlockCacheKey(name,offset),cacheFormatBlock);
}
