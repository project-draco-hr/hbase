{
  StringBuilder builder=new StringBuilder("[");
  for (  HRegionLocation loc : locations) {
    if (loc != null) {
      if (builder.length() > 1) {
        builder.append(", ");
      }
      builder.append(loc);
    }
  }
  builder.append("]");
  return builder.toString();
}
