{
  int res=upgradeNamespace();
  if (res == 0)   return upgradeZnodes();
 else {
    LOG.warn("Namespace upgrade returned: " + res + ", expected 0. Aborting the upgrade");
    throw new Exception("Unexpected return code from Namespace upgrade");
  }
}
