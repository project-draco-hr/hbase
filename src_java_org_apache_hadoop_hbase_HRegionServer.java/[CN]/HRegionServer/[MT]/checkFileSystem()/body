{
  boolean fsOk=true;
  if (!FSUtils.isFileSystemAvailable(fs)) {
    LOG.fatal("Shutting down HRegionServer: file system not available");
    abortRequested=true;
    stopRequested=true;
    fsOk=false;
  }
  return fsOk;
}
