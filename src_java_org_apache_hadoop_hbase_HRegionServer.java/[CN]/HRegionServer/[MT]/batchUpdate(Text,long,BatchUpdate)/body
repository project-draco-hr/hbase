{
  checkOpen();
  this.requestCount.incrementAndGet();
  HRegion region=getRegion(regionName);
  try {
    region.batchUpdate(timestamp,b);
  }
 catch (  IOException e) {
    checkFileSystem();
    throw e;
  }
}
