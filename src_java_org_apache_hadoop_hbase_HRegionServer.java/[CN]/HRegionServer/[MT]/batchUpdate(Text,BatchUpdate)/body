{
  checkOpen();
  this.requestCount.incrementAndGet();
  HRegion region=getRegion(regionName);
  try {
    region.batchUpdate(b);
  }
 catch (  IOException e) {
    checkFileSystem();
    throw e;
  }
}
