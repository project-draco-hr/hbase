{
  super(null,comparator,memStoreLAB,CompactingMemStore.DEEP_OVERHEAD_PER_PIPELINE_CELL_ARRAY_ITEM,ClassSize.CELL_ARRAY_MAP_ENTRY);
  CellSet cs=createCellArrayMapSet(numOfCells,iterator);
  this.setCellSet(null,cs);
  this.type=type;
  TimeRangeTracker trt=getTimeRangeTracker();
  this.timeRange=trt == null ? null : trt.toTimeRange();
}
