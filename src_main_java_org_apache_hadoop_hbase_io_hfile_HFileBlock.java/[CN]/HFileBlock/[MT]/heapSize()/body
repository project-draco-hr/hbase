{
  long size=ClassSize.align(SCHEMA_CONFIGURED_UNALIGNED_HEAP_SIZE + 2 * ClassSize.REFERENCE + 3 * Bytes.SIZEOF_INT + 2 * Bytes.SIZEOF_LONG);
  if (buf != null) {
    size+=ClassSize.align(buf.capacity() + BYTE_BUFFER_HEAP_SIZE);
  }
  return ClassSize.align(size);
}
