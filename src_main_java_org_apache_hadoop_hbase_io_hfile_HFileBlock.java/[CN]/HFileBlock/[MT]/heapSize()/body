{
  long size=ClassSize.align(ClassSize.OBJECT + 2 * ClassSize.REFERENCE + 3 * Bytes.SIZEOF_INT + 2 * Bytes.SIZEOF_LONG);
  if (buf != null) {
    size+=ClassSize.align(buf.capacity() + BYTE_BUFFER_HEAP_SIZE);
  }
  size+=super.heapSize() - ClassSize.OBJECT;
  return size;
}
