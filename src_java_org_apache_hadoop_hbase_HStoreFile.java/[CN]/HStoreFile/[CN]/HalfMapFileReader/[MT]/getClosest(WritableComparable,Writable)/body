{
  if (this.top) {
    if (key.compareTo(this.midkey) < 0) {
      return this.midkey;
    }
  }
 else   if (key.compareTo(this.midkey) >= 0) {
    return null;
  }
  return super.getClosest(key,val);
}
