{
  boolean success=fs.rename(getMapFilePath(),hsf.getMapFilePath());
  if (!success) {
    LOG.warn("Failed rename of " + getMapFilePath() + " to "+ hsf.getMapFilePath());
    return success;
  }
  success=fs.rename(getInfoFilePath(),hsf.getInfoFilePath());
  if (!success) {
    LOG.warn("Failed rename of " + getInfoFilePath() + " to "+ hsf.getInfoFilePath());
  }
  return success;
}
