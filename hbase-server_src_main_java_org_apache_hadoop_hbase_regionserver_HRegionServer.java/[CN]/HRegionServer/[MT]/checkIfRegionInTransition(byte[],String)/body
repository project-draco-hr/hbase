{
  if (this.regionsInTransitionInRS.containsKey(regionEncodedName)) {
    boolean openAction=this.regionsInTransitionInRS.get(regionEncodedName);
    throw new RegionAlreadyInTransitionException("Received:" + currentAction + " for the region:"+ Bytes.toString(regionEncodedName)+ " ,which we are already trying to "+ (openAction ? OPEN : CLOSE)+ ".");
  }
}
