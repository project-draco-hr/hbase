{
  Cell[] results=r.get(k,new Text(family),Integer.MAX_VALUE);
  for (int j=0; j < results.length; j++) {
    Text tmp=new Text(results[j].getValue());
    assertEquals(k.toString(),tmp.toString());
  }
}
