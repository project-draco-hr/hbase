{
  super(fs,dirName,new HStoreKey.HStoreKeyWritableComparator(),conf,false);
  this.blockCacheEnabled=blockCacheEnabled;
  open(fs,dirName,new HStoreKey.HStoreKeyWritableComparator(),conf);
  midKey();
}
