{
  if (rowCache == null) {
    int o=getRowOffset();
    short l=getRowLength();
    byte local[]=new byte[l];
    System.arraycopy(getBuffer(),o,local,0,l);
    rowCache=local;
  }
  return rowCache;
}
