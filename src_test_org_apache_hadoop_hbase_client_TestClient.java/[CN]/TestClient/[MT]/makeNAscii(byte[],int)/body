{
  if (n > 256) {
    return makeNBig(base,n);
  }
  byte[][] ret=new byte[n][];
  for (int i=0; i < n; i++) {
    byte[] tail=Bytes.toBytes(new Integer(i).toString());
    ret[i]=Bytes.add(base,tail);
  }
  return ret;
}
