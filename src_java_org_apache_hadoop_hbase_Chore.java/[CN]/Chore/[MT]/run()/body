{
  try {
    while (!initialChore()) {
      this.sleeper.sleep();
    }
    this.sleeper.sleep();
    while (!this.stop.get()) {
      long startTime=System.currentTimeMillis();
      chore();
      this.sleeper.sleep(startTime);
    }
  }
  finally {
    LOG.info(getName() + " exiting");
  }
}
