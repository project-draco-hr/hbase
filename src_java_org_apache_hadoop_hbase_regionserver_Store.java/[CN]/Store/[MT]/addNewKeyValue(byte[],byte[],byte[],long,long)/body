{
  long newValue=value + amount;
  KeyValue newKv=new KeyValue(row,f,qualifier,System.currentTimeMillis(),Bytes.toBytes(newValue));
  add(newKv);
  return new ValueAndSize(newValue,newKv.heapSize());
}
