{
  int result=s.seekTo(kv.getBuffer(),kv.getKeyOffset(),kv.getKeyLength());
  if (result < 0) {
    if (!s.seekTo()) {
      return false;
    }
  }
 else   if (result > 0) {
    if (!s.next()) {
      return false;
    }
  }
  return true;
}
