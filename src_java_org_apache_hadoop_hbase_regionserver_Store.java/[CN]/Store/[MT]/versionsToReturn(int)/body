{
  if (wantedVersions <= 0) {
    throw new IllegalArgumentException("Number of versions must be > 0");
  }
  return wantedVersions > this.family.getMaxVersions() && wantedVersions != HConstants.ALL_VERSIONS ? this.family.getMaxVersions() : wantedVersions;
}
