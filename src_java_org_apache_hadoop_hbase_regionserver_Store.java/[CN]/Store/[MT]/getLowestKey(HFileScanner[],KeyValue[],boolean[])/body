{
  int lowestKey=-1;
  for (int i=0; i < rdrs.length; i++) {
    if (done[i]) {
      continue;
    }
    if (lowestKey < 0) {
      lowestKey=i;
    }
 else {
      if (this.comparator.compare(keys[i],keys[lowestKey]) < 0) {
        lowestKey=i;
      }
    }
  }
  return lowestKey;
}
