{
  THLogKey key=new THLogKey(regionInfo.getRegionName(),regionInfo.getTableDesc().getName(),-1,now,txOp,transactionId);
  WALEdit e=new WALEdit();
  e.add(new KeyValue(new byte[0],0,0));
  super.append(regionInfo,key,e,regionInfo.isMetaRegion());
}
