{
  SequenceFile.Writer writer=SequenceFile.createWriter(fs,conf,path,String.class,String.class);
  try {
    for (    String fileName : filesNames) {
      writer.append(fileName,MobConstants.EMPTY_STRING);
    }
  }
  finally {
    IOUtils.closeStream(writer);
  }
}
