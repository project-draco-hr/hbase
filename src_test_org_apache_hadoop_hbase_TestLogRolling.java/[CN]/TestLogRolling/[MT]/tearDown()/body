{
  try {
    super.tearDown();
    if (cluster != null) {
      cluster.shutdown();
    }
    StaticTestEnvironment.shutdownDfs(dfs);
  }
 catch (  Exception e) {
    LOG.fatal("error in tearDown",e);
    throw e;
  }
}
