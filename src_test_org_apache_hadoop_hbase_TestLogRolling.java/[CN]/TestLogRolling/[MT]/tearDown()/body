{
  super.tearDown();
  if (cluster != null) {
    cluster.shutdown();
  }
  assertEquals(0,countLogFiles(true));
  if (dfs != null) {
    FileSystem fs=dfs.getFileSystem();
    try {
      dfs.shutdown();
    }
  finally {
      fs.close();
    }
  }
}
