{
  ZKUtil.createWithParents(this.zookeeper,this.stateZnode);
  byte[] stateBytes=(newState == true) ? ReplicationZookeeper.ENABLED_ZNODE_BYTES : ReplicationZookeeper.DISABLED_ZNODE_BYTES;
  ZKUtil.setData(this.zookeeper,this.stateZnode,stateBytes);
}
