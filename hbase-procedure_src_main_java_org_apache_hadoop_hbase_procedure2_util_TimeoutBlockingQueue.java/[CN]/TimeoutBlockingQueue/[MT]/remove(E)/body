{
  if (e == null)   return false;
  lock.lock();
  try {
    for (int i=0; i < objects.length; ++i) {
      if (e.equals(objects[i])) {
        objects[i]=null;
        return true;
      }
    }
    return false;
  }
  finally {
    lock.unlock();
  }
}
