{
  if (LOG.isDebugEnabled()) {
    LOG.debug("PUT " + uriInfo.getAbsolutePath());
  }
  if (model.getName() != tableName) {
    LOG.warn("table name mismatch: path='" + tableName + "', schema='"+ model.getName()+ "'");
  }
  return update(model,true,uriInfo);
}
