{
  try {
    if (key == null) {
      close();
      return false;
    }
    this.firstOnNextRow.set(key);
    return cacheNextRow();
  }
 catch (  Exception e) {
    close();
    return false;
  }
}
