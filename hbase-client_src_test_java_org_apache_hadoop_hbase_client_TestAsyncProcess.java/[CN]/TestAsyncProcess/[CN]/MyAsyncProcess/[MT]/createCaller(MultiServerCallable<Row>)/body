{
  final MultiResponse mr=createMultiResponse(callable.getLocation(),callable.getMulti());
  return new RpcRetryingCaller<MultiResponse>(){
    @Override public MultiResponse callWithoutRetries(    RetryingCallable<MultiResponse> callable) throws IOException, RuntimeException {
      return mr;
    }
  }
;
}
