{
  Put p;
  if (!success) {
    p=new Put(FAILS);
  }
 else   if (reg1) {
    p=new Put(DUMMY_BYTES_1);
  }
 else {
    p=new Put(DUMMY_BYTES_2);
  }
  p.add(DUMMY_BYTES_1,DUMMY_BYTES_1,DUMMY_BYTES_1);
  return p;
}
