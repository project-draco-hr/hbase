{
  if (!assigned) {
    throw new IllegalStateException("Cannot set a region as pending if it has not been assigned. Region: " + Bytes.toString(regionName));
  }
  this.unassigned=false;
  this.assigned=false;
  this.pending=true;
  this.closing=false;
}
