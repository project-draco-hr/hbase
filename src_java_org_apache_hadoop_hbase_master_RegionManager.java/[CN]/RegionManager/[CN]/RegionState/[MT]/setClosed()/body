{
  if (!closing) {
    throw new IllegalStateException("Cannot set a region to be closed if it was not already marked as" + " closing. State: " + toString());
  }
  this.closing=false;
  this.closed=true;
}
