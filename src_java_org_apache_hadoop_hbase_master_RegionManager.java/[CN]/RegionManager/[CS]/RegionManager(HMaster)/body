{
  this.master=master;
  this.historian=RegionHistorian.getInstance();
  this.maxAssignInOneGo=this.master.getConfiguration().getInt("hbase.regions.percheckin",10);
  this.slop=this.master.getConfiguration().getFloat("hbase.regions.slop",(float)0.1);
  rootScannerThread=new RootScanner(master,this);
  metaScannerThread=new MetaScanner(master,this);
  unassignRootRegion();
}
