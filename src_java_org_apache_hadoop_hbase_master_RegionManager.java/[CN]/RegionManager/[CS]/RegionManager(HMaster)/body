{
  this.master=master;
  this.maxAssignInOneGo=this.master.conf.getInt("hbase.regions.percheckin",10);
  rootScannerThread=new RootScanner(master,this);
  metaScannerThread=new MetaScanner(master,this);
  unassignRootRegion();
}
