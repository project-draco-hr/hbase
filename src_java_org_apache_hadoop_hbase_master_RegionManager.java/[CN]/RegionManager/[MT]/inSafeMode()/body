{
  if (safeMode) {
    if (isInitialMetaScanComplete() && unassignedRegions.size() == 0 && pendingRegions.size() == 0) {
      safeMode=false;
      LOG.info("exiting safe mode");
    }
 else {
      LOG.info("in safe mode");
    }
  }
  return safeMode;
}
