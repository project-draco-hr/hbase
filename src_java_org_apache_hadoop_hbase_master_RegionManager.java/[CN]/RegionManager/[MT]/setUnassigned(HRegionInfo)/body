{
synchronized (this.unassignedRegions) {
    if (!this.unassignedRegions.containsKey(info) && !this.pendingRegions.contains(info.getRegionName())) {
      this.unassignedRegions.put(info,ZERO_L);
    }
  }
}
