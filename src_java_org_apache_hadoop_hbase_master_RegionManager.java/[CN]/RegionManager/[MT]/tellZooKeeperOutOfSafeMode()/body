{
  for (int attempt=0; attempt < zooKeeperNumRetries; ++attempt) {
    if (zooKeeperWrapper.writeOutOfSafeMode()) {
      return true;
    }
    sleep(attempt);
  }
  LOG.error("Failed to tell ZooKeeper we're out of safe mode after " + zooKeeperNumRetries + " retries");
  return false;
}
