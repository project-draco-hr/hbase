{
  boolean status=false;
  try {
    status=this.row.compareTo(other.row) == 0 && extractFamily(this.column).compareTo(extractFamily(other.getColumn())) == 0;
  }
 catch (  IOException e) {
  }
  return status;
}
