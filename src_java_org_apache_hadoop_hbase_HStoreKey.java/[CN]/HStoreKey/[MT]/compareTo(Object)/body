{
  HStoreKey other=(HStoreKey)o;
  int result=compareTwoRowKeys(this.regionInfo,this.row,other.row);
  if (result != 0) {
    return result;
  }
  result=this.column == null && other.column == null ? 0 : this.column == null && other.column != null ? -1 : this.column != null && other.column == null ? 1 : Bytes.compareTo(this.column,other.column);
  if (result != 0) {
    return result;
  }
  if (this.timestamp < other.timestamp) {
    result=1;
  }
 else   if (this.timestamp > other.timestamp) {
    result=-1;
  }
  return result;
}
