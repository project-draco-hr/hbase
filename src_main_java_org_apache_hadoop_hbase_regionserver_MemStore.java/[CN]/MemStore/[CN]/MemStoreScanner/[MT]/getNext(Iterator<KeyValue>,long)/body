{
  KeyValue ret=null;
  while (ret == null && it.hasNext()) {
    KeyValue v=it.next();
    if (v.getMemstoreTS() <= readPoint) {
      ret=v;
    }
    numIterReseek--;
    if (numIterReseek == 0) {
      break;
    }
  }
  return ret;
}
