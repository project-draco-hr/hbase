{
  KeyValue result=null;
  SortedSet<KeyValue> tail=key == null ? set : set.tailSet(key);
  for (  KeyValue kv : tail) {
    if (comparator.compareRows(kv,key) <= 0)     continue;
    result=kv;
    break;
  }
  return result;
}
