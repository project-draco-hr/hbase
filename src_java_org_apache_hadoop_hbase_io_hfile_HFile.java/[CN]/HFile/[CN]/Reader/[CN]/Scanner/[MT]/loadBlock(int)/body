{
  if (block == null) {
    block=reader.readBlock(bloc);
    currBlock=bloc;
    blockFetches++;
  }
 else {
    if (bloc != currBlock) {
      block=reader.readBlock(bloc);
      currBlock=bloc;
      blockFetches++;
    }
 else {
      block.rewind();
    }
  }
}
