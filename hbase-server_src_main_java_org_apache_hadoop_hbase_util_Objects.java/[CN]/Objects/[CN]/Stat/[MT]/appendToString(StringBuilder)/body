{
  super.appendToString(out);
  out.append(" [ ");
  if (count > 0) {
    out.append("min=").append(min).append(" max=").append(max).append(" avg=").append((int)(total / count));
  }
 else {
    out.append("none");
  }
  out.append(" ]");
}
