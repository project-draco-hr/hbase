{
  if (this.closed) {
    return;
  }
  delayedClosing.stop("Closing connection");
  closeMaster();
  closeZooKeeperWatcher();
  this.servers.clear();
  this.rpcEngine.close();
  this.closed=true;
}
