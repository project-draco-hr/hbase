{
synchronized (readerLock) {
synchronized (writerLock) {
      while (numWriters > 0) {
        try {
          writerLock.wait();
        }
 catch (        InterruptedException ie) {
        }
      }
      numReaders++;
      readerLock.notifyAll();
    }
  }
}
