{
  String rowStr=Bytes.toString(kv.getBuffer(),kv.getRowOffset(),kv.getRowLength());
  String qualStr=Bytes.toString(kv.getBuffer(),kv.getQualifierOffset(),kv.getQualifierLength());
  return rowStr + "_" + qualStr;
}
