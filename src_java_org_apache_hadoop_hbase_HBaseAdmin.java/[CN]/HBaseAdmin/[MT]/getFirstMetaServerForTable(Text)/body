{
  SortedMap<Text,HRegionLocation> metaservers=connection.getTableServers(META_TABLE_NAME);
  return metaservers.get((metaservers.containsKey(tableName)) ? tableName : metaservers.headMap(tableName).lastKey());
}
