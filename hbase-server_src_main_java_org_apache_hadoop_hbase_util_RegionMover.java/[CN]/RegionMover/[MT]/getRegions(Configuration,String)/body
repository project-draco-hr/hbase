{
  HConnection conn=HConnectionManager.createConnection(conf);
  List<HRegionInfo> regions=ProtobufUtil.getOnlineRegions(conn.getAdmin(ServerName.valueOf(server)));
  conn.close();
  return regions;
}
