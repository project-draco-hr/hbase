{
  boolean isOk=true;
  if (rc.getThreadsCount() > MAX_THREADS_COUNT) {
    LOG.error(tagLine + ": too many threads used. We use " + rc.getThreadsCount()+ " our max is "+ MAX_THREADS_COUNT);
    isOk=false;
  }
  return isOk;
}
