{
  FileStatus[] files=FSUtils.listStatus(fs,getStoreDir(familyName),new PathFilter(){
    @Override public boolean accept(    Path path){
      return StoreFileInfo.isReference(path);
    }
  }
);
  return files != null && files.length > 0;
}
