{
switch (fieldID) {
case TABLENAME:
    return getTableName();
case STARTROW:
  return getStartRow();
case STOPROW:
return getStopRow();
case COLUMNS:
return getColumns();
case TIMESTAMP:
return new Long(getTimestamp());
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
