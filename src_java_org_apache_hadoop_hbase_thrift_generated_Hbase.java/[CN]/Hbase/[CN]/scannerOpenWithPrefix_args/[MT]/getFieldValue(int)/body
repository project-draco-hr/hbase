{
switch (fieldID) {
case TABLENAME:
    return getTableName();
case STARTANDPREFIX:
  return getStartAndPrefix();
case COLUMNS:
return getColumns();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
