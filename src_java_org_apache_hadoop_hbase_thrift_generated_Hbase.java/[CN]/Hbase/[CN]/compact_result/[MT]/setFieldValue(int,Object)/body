{
switch (fieldID) {
case IO:
    if (value == null) {
      unsetIo();
    }
 else {
      setIo((IOError)value);
    }
  break;
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
