{
switch (fieldID) {
case SUCCESS:
    return new Long(getSuccess());
case IO:
  return getIo();
case IA:
return getIa();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
