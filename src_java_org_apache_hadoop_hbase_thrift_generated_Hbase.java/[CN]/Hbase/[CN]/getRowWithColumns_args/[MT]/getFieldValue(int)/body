{
switch (fieldID) {
case TABLENAME:
    return getTableName();
case ROW:
  return getRow();
case COLUMNS:
return getColumns();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
