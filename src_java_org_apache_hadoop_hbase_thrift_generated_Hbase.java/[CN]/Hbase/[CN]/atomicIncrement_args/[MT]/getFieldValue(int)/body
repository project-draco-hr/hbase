{
switch (fieldID) {
case TABLENAME:
    return getTableName();
case ROW:
  return getRow();
case COLUMN:
return getColumn();
case VALUE:
return new Long(getValue());
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
