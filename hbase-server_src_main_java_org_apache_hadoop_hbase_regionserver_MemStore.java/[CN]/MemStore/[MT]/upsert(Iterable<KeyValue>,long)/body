{
  this.lock.readLock().lock();
  try {
    long size=0;
    for (    KeyValue kv : kvs) {
      size+=upsert(kv,readpoint);
    }
    return size;
  }
  finally {
    this.lock.readLock().unlock();
  }
}
