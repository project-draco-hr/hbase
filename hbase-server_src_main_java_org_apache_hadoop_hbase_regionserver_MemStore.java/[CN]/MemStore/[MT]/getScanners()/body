{
  this.lock.readLock().lock();
  try {
    return Collections.<KeyValueScanner>singletonList(new MemStoreScanner(MultiVersionConsistencyControl.getThreadReadPoint()));
  }
  finally {
    this.lock.readLock().unlock();
  }
}
