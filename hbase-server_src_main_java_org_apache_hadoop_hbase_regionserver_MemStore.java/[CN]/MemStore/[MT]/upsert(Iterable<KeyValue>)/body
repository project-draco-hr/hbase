{
  this.lock.readLock().lock();
  try {
    long size=0;
    for (    KeyValue kv : kvs) {
      kv.setMemstoreTS(0);
      size+=upsert(kv);
    }
    return size;
  }
  finally {
    this.lock.readLock().unlock();
  }
}
