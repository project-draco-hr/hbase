{
  if (!hasEventTypeCode()) {
    return false;
  }
  if (!hasRegionName()) {
    return false;
  }
  if (!hasCreateTime()) {
    return false;
  }
  if (hasOriginServerName()) {
    if (!getOriginServerName().isInitialized()) {
      return false;
    }
  }
  return true;
}
