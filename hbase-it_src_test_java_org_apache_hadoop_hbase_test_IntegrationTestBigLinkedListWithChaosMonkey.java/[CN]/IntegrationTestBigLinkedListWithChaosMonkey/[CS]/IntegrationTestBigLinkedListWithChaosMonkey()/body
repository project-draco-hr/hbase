{
  super();
  Configuration conf=getConf();
  if (conf != null) {
    conf.set(TABLE_NAME_KEY,"IntegrationTestBigLinkedListWithChaosMonkey");
  }
 else {
    this.getTestingUtil().getConfiguration().set(TABLE_NAME_KEY,"IntegrationTestBigLinkedListWithChaosMonkey");
    setConf(conf);
  }
}
