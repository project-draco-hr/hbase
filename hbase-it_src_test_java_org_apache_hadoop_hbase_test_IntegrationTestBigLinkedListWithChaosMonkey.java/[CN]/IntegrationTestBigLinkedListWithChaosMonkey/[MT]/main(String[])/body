{
  IntegrationTestBigLinkedListWithChaosMonkey test=new IntegrationTestBigLinkedListWithChaosMonkey();
  IntegrationTestingUtility.setUseDistributedCluster(test.getTestingUtil().getConfiguration());
  test.NUM_SLAVES_BASE=3;
  test.setUp();
  int ret=-1;
  try {
    ret=ToolRunner.run(test.getTestingUtil().getConfiguration(),test,args);
  }
  finally {
    test.tearDown();
  }
  System.exit(ret);
}
