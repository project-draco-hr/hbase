{
  if (!(other instanceof KeyValue)) {
    return false;
  }
  KeyValue kv=(KeyValue)other;
  boolean result=Bytes.BYTES_RAWCOMPARATOR.compare(getBuffer(),getKeyOffset(),getKeyLength(),kv.getBuffer(),kv.getKeyOffset(),kv.getKeyLength()) == 0;
  return result;
}
