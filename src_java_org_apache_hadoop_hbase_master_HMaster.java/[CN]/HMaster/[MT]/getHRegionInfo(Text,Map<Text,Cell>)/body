{
  Cell regioninfo=map.get(COL_REGIONINFO);
  if (regioninfo == null) {
    LOG.warn(COL_REGIONINFO.toString() + " is empty for row: " + row+ "; has keys: "+ map.keySet().toString());
    return null;
  }
  return (HRegionInfo)Writables.getWritable(regioninfo.getValue(),new HRegionInfo());
}
