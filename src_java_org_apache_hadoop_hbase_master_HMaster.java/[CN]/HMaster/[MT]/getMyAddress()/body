{
  String addressStr=DNS.getDefaultHost(conf.get("hbase.master.dns.interface","default"),conf.get("hbase.master.dns.nameserver","default"));
  addressStr+=":" + this.conf.get(MASTER_PORT,Integer.toString(DEFAULT_MASTER_PORT));
  return addressStr;
}
