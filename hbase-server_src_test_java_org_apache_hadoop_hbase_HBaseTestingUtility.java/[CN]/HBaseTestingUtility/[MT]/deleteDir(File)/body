{
  if (dir != null && dir.exists()) {
    if (!FSUtils.deleteDirectory(FileSystem.getLocal(this.conf),new Path(dir.getAbsolutePath()))) {
      LOG.warn("Failed delete of " + dir.toString());
    }
 else {
      return true;
    }
  }
  return false;
}
