{
  TEST_UTIL.startMiniCluster(3);
  Path repLogPath=new Path(TEST_UTIL.getTestDir(),ReplicationSink.getRepLogPath("test_rep_sink"));
  SINK=new ReplicationSink(conf,STOPPER,TEST_UTIL.getTestDir(),TEST_UTIL.getDFSCluster().getFileSystem(),1000);
  SINK.start();
  table1=TEST_UTIL.createTable(TABLE_NAME1,FAM_NAME1);
  table2=TEST_UTIL.createTable(TABLE_NAME2,FAM_NAME2);
}
