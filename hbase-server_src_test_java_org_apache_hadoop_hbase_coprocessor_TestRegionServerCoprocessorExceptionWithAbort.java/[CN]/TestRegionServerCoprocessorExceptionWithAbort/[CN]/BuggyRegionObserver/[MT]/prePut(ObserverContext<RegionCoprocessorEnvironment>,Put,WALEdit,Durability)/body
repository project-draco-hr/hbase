{
  TableName tableName=c.getEnvironment().getRegion().getRegionInfo().getTableName();
  if (TABLE_NAME.equals(tableName) && Bytes.equals(put.getRow(),ROW)) {
    throw new NullPointerException("Buggy coprocessor: " + put);
  }
}
