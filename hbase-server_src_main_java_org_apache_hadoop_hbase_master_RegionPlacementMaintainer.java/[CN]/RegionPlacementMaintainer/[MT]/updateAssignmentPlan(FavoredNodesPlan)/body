{
  LOG.info("Start to update the new assignment plan for the META table and" + " the region servers");
  updateAssignmentPlanToMeta(plan);
  updateAssignmentPlanToRegionServers(plan);
  LOG.info("Finish to update the new assignment plan for the META table and" + " the region servers");
}
