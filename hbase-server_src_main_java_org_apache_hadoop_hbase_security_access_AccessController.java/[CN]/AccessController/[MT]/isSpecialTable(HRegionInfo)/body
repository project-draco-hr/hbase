{
  byte[] tableName=regionInfo.getTableName();
  return Arrays.equals(tableName,AccessControlLists.ACL_TABLE_NAME) || Arrays.equals(tableName,Bytes.toBytes("-ROOT-")) || Arrays.equals(tableName,Bytes.toBytes(".META."));
}
