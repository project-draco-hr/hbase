{
  for (int i=0; i < columns.length; i++) {
    try {
      byte[][] split=KeyValue.parseColumn(columns[i]);
      addColumn(split[0],split[1]);
    }
 catch (    Exception e) {
    }
  }
  return this;
}
