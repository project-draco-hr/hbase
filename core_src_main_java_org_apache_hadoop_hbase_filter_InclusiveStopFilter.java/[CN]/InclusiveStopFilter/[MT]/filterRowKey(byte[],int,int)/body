{
  if (buffer == null) {
    if (this.stopRowKey == null) {
      return true;
    }
    return false;
  }
  int cmp=Bytes.compareTo(stopRowKey,0,stopRowKey.length,buffer,offset,length);
  if (cmp < 0) {
    done=true;
  }
  return done;
}
