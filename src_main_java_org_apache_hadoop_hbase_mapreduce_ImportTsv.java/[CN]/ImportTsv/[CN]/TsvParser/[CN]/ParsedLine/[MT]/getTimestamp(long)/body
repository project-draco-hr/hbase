{
  if (!hasTimestamp()) {
    return ts;
  }
  try {
    return Long.parseLong(Bytes.toString(lineBytes,getColumnOffset(timestampKeyColumnIndex),getColumnLength(timestampKeyColumnIndex)));
  }
 catch (  NumberFormatException nfe) {
    throw new BadTsvLineException("Invalid timestamp");
  }
}
