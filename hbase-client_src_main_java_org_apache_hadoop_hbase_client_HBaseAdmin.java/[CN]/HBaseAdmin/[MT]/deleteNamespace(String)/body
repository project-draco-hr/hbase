{
  executeCallable(new MasterAdminCallable<Void>(getConnection()){
    @Override public Void call() throws Exception {
      masterAdmin.deleteNamespace(null,MasterAdminProtos.DeleteNamespaceRequest.newBuilder().setNamespaceName(name).build());
      return null;
    }
  }
);
}
