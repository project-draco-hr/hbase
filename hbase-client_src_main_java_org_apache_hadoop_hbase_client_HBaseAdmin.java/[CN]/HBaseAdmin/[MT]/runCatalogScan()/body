{
  return executeCallable(new MasterCallable<Integer>(getConnection(),getRpcControllerFactory()){
    @Override protected Integer call(    PayloadCarryingRpcController rpcController) throws Exception {
      return master.runCatalogScan(rpcController,RequestConverter.buildCatalogScanRequest()).getScanResult();
    }
  }
);
}
