{
  try {
    this.connection.getMaster().updateMasterConfiguration(null,UpdateMasterConfigurationRequest.getDefaultInstance());
  }
 catch (  ServiceException e) {
    throw ProtobufUtil.getRemoteException(e);
  }
}
