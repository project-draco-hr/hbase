{
  if (o instanceof TextSequence) {
    TextSequence that=(TextSequence)o;
    if (this == that) {
      return 0;
    }
    return WritableComparator.compareBytes(this.delegatee.getBytes(),this.start,this.getLength(),that.delegatee.getBytes(),that.start,that.getLength());
  }
  Text that=(Text)o;
  return WritableComparator.compareBytes(this.delegatee.getBytes(),this.start,this.getLength(),that.getBytes(),0,that.getLength());
}
