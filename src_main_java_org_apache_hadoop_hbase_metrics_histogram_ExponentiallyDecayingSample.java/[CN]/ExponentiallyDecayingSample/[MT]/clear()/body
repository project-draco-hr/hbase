{
  lockForRescale();
  try {
    values.clear();
    count.set(0);
    this.startTime=CURRENT_TICK;
    nextScaleTime.set(System.nanoTime() + RESCALE_THRESHOLD);
  }
  finally {
    unlockForRescale();
  }
}
