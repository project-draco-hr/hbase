{
  return new RpcRetryingCaller<Void>(pauseTime,retries,interceptor){
    @Override public Void callWithRetries(    RetryingCallable<Void> callable,    int callTimeout) throws IOException, RuntimeException {
      Void ret=super.callWithRetries(callable,callTimeout);
      return ret;
    }
  }
;
}
