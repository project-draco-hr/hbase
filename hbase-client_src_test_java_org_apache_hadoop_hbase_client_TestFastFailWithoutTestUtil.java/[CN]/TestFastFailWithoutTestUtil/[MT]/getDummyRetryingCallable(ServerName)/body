{
  return new RegionServerCallable<T>(null,null,null){
    @Override public T call(    int callTimeout) throws Exception {
      return null;
    }
    @Override protected HRegionLocation getLocation(){
      return new HRegionLocation(null,serverName);
    }
  }
;
}
