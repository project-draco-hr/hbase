{
  try {
    unlockHbck();
  }
 catch (  Exception io) {
    LOG.warn(io);
  }
 finally {
    IOUtils.closeQuietly(admin);
    IOUtils.closeQuietly(meta);
    IOUtils.closeQuietly(connection);
  }
}
