{
  int familyLength=v.getFamilyLength();
  if (familyLength > 0) {
    if (doCompare(this.compareOp,this.comparator,v.getBuffer(),v.getFamilyOffset(),familyLength)) {
      return ReturnCode.SKIP;
    }
  }
  return ReturnCode.INCLUDE;
}
