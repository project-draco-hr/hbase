{
  return execute(new MasterAdminCallable<List<SnapshotDescription>>(){
    @Override public List<SnapshotDescription> call() throws ServiceException {
      return masterAdmin.listSnapshots(null,ListSnapshotRequest.newBuilder().build()).getSnapshotsList();
    }
  }
);
}
