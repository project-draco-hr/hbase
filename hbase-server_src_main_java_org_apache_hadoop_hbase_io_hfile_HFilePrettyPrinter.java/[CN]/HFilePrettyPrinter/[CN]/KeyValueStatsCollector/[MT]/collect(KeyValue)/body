{
  valLen.update(kv.getValueLength());
  if (prevKV != null && KeyValue.COMPARATOR.compareRows(prevKV,kv) != 0) {
    collectRow();
  }
  curRowBytes+=kv.getLength();
  curRowKeyLength=kv.getKeyLength();
  curRowCols++;
  prevKV=kv;
}
