{
  if (prevKV == null)   return "no data available for statistics";
  simpleReporter.shutdown();
  simpleReporter.run();
  metricsRegistry.shutdown();
  return metricsOutput.toString() + "Key of biggest row: " + Bytes.toStringBinary(biggestRow);
}
