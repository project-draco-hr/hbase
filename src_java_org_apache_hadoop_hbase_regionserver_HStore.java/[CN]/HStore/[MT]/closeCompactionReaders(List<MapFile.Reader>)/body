{
  for (  MapFile.Reader r : rdrs) {
    try {
      if (r != null) {
        r.close();
      }
    }
 catch (    IOException e) {
      LOG.warn("Exception closing reader for " + this.storeNameStr,e);
    }
  }
}
