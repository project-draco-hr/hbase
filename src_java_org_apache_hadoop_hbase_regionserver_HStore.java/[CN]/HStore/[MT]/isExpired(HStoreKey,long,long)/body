{
  boolean result=ttl != HConstants.FOREVER && now > hsk.getTimestamp() + ttl;
  if (LOG.isDebugEnabled()) {
    LOG.debug("rowAtOrBeforeCandidate 1:" + hsk + ": expired, skipped");
  }
  return result;
}
