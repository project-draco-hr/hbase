{
  super.prepareWithTableLock();
  if (masterServices.getAssignmentManager().getTableStateManager().isTableState(this.htd.getTableName(),TableState.State.ENABLED) && this.htd.getRegionReplication() != getTableDescriptor().getHTableDescriptor().getRegionReplication()) {
    throw new IOException("REGION_REPLICATION change is not supported for enabled tables");
  }
}
