{
  for (  Entry<String,String> e : c) {
    set(e.getKey(),e.getValue());
  }
  LOG.warn("instantinating HBaseConfiguration() is deprecated. Please use " + "HBaseConfiguration#create(conf) to construct a plain Configuration");
}
