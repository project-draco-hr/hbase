{
  super.setUp();
  try {
    this.miniHdfs=new MiniDFSCluster(this.conf,1,true,null);
    this.conf.set(HConstants.HBASE_DIR,this.miniHdfs.getFileSystem().getHomeDirectory().toString());
  }
 catch (  Exception e) {
    LOG.fatal("error starting MiniDFSCluster",e);
    throw e;
  }
}
