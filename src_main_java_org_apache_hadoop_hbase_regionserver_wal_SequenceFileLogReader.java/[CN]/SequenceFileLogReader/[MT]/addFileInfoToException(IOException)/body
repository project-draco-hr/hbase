{
  long pos=-1;
  try {
    pos=getPosition();
  }
 catch (  IOException e) {
    Log.warn("Failed getting position to add to throw",e);
  }
  return new IOException((this.path == null ? "" : this.path.toString()) + ", pos=" + pos+ ", edit="+ this.edit,ioe);
}
