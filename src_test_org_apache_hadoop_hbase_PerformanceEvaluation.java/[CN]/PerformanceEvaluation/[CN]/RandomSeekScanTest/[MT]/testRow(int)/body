{
  Scanner s=this.table.getScanner(new byte[][]{COLUMN_NAME},getRandomRow(this.rand,this.totalRows),new WhileMatchRowFilter(new PageRowFilter(120)));
  for (RowResult rr=null; (rr=s.next()) != null; ) {
  }
  s.close();
}
