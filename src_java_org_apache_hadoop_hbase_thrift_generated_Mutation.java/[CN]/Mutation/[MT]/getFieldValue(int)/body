{
switch (fieldID) {
case ISDELETE:
    return new Boolean(isIsDelete());
case COLUMN:
  return getColumn();
case VALUE:
return getValue();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
