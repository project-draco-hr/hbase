{
  try {
    parseConfig();
  }
 catch (  Exception e) {
    e.printStackTrace();
    System.exit(-1);
  }
  if (ServerConfig.isStandalone()) {
    ZooKeeperServerMain.main(args);
  }
 else {
    QuorumPeerMain.runPeerFromConfig();
  }
}
