{
  System.out.println("Testing " + map.getClass());
  for (int i=0; i < 1000000; i++) {
    map.put(new Integer(i),new Integer(i));
  }
  System.out.println(map.size());
  byte[] block=new byte[849 * 1024 * 1024];
  System.out.println(map.size());
}
