{
  HFileContext meta=new HFileContext();
  meta.setIncludesMvcc(this.includesMemstoreTS);
  meta.setUsesHBaseChecksum(trailer.getMinorVersion() >= MINOR_VERSION_WITH_CHECKSUM);
  meta.setCompressAlgo(this.compressAlgo);
  return meta;
}
