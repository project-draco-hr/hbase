{
  if (columnName == null) {
    return;
  }
  if (!regionInfo.getTableDesc().hasFamily(columnName)) {
    throw new NoSuchColumnFamilyException("Column family on " + Bytes.toString(columnName) + " does not exist in region "+ this+ " in table "+ regionInfo.getTableDesc());
  }
}
