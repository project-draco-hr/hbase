{
  if (column == null) {
    return;
  }
  if (!regionInfo.getTableDesc().hasFamily(column)) {
    throw new NoSuchColumnFamilyException("Column family on " + Bytes.toString(column) + " does not exist in region "+ this+ " in table "+ regionInfo.getTableDesc());
  }
}
