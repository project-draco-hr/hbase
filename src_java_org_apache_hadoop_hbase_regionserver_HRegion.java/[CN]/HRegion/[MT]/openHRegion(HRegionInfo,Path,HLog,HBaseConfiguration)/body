{
  if (LOG.isDebugEnabled()) {
    LOG.debug("Opening region: " + info);
  }
  return new HRegion(HTableDescriptor.getTableDir(rootDir,info.getTableDesc().getName()),log,FileSystem.get(conf),conf,info,null,null);
}
