{
  if (!hasColumnFamily()) {
    return false;
  }
  if (!hasColumnQualifier()) {
    return false;
  }
  if (!hasCompareOp()) {
    return false;
  }
  if (!hasComparator()) {
    return false;
  }
  return true;
}
