{
  Cell searchTerm=KeyValue.createFirstOnRow(CellUtil.getRowArray(kvs[0]),family,qualifier);
  int pos=Arrays.binarySearch(kvs,searchTerm,KeyValue.COMPARATOR);
  if (pos < 0) {
    pos=(pos + 1) * -1;
  }
  if (pos == kvs.length) {
    return -1;
  }
  return pos;
}
