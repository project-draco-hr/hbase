{
  Path tableInfoPath=getTableInfoPath(rootdir,hTableDescriptor.getNameAsString());
  writeTableDescriptor(fs,hTableDescriptor,tableInfoPath.getParent(),true);
  LOG.info("Updated tableinfo=" + tableInfoPath + " to "+ hTableDescriptor.toString());
}
