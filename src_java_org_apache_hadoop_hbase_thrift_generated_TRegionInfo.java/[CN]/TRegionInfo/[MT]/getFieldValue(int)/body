{
switch (fieldID) {
case STARTKEY:
    return getStartKey();
case ENDKEY:
  return getEndKey();
case ID:
return getId();
case NAME:
return getName();
case VERSION:
return getVersion();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
