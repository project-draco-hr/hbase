{
  changeFAR(this.filter.filter(rowKey,colKey,data));
  boolean result=filterAllRemaining();
  if (LOG.isDebugEnabled()) {
    LOG.debug("Filter on rowKey:" + rowKey + ", colKey: "+ colKey+ ", data: "+ data+ ".  Result = "+ result);
  }
  return result;
}
