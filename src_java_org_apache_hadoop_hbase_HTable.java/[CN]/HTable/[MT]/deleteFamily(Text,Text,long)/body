{
  checkClosed();
  getRegionServerWithRetries(new ServerCallable<Boolean>(row){
    public Boolean call() throws IOException {
      server.deleteFamily(location.getRegionInfo().getRegionName(),row,family,timestamp);
      return null;
    }
  }
);
}
