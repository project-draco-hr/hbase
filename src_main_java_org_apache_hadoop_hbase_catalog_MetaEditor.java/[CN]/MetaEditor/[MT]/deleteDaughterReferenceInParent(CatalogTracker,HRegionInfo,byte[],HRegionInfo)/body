{
  Delete delete=new Delete(parent.getRegionName());
  delete.deleteColumns(HConstants.CATALOG_FAMILY,qualifier);
  deleteMetaTable(catalogTracker,delete);
  LOG.info("Deleted daughter reference " + daughter.getRegionNameAsString() + ", qualifier="+ Bytes.toStringBinary(qualifier)+ ", from parent "+ parent.getRegionNameAsString());
}
