{
  Put put=new Put(regionInfo.getRegionName());
  addLocation(put,sn);
  server.put(catalogRegionName,put);
  LOG.info("Updated row " + regionInfo.getRegionNameAsString() + " in region "+ Bytes.toString(catalogRegionName)+ " with "+ "serverName="+ sn.toString());
}
