{
  catalogTracker.waitForMetaServerConnectionDefault().put(CatalogTracker.META_REGION,makePutFromRegionInfo(regionInfo));
  LOG.info("Added region " + regionInfo.getRegionNameAsString() + " to META");
}
