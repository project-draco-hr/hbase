{
  try {
    handleCoprocessorThrowable(env,e);
  }
 catch (  IOException ioe) {
    LOG.warn("handleCoprocessorThrowable() threw an IOException while attempting to handle Throwable " + e + ". Ignoring.",e);
  }
}
