{
  TEST_UTIL.shutdownMiniCluster();
  long remainingPrefetchedSize=RegionScannerHolder.getPrefetchedResultSize();
  assertEquals("All prefetched results should be gone",0,remainingPrefetchedSize);
}
