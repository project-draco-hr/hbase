{
  int tsOffset=getTimestampOffset();
  if (Bytes.compareTo(now,0,Bytes.SIZEOF_LONG,this.bytes,tsOffset,Bytes.SIZEOF_LONG) < 0) {
    System.arraycopy(now,0,this.bytes,tsOffset,Bytes.SIZEOF_LONG);
    return true;
  }
  return false;
}
