{
  int o=getColumnOffset();
  int l=getColumnLength(o);
  int familylength=getFamilyLength(o);
  return Bytes.toString(this.bytes,o,familylength) + COLUMN_FAMILY_DELIMITER + Bytes.toString(this.bytes,o + familylength,l - familylength);
}
