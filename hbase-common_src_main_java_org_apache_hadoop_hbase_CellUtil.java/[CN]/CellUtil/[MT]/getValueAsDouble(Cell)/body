{
  if (cell instanceof ByteBufferedCell) {
    return ByteBufferUtils.toDouble(((ByteBufferedCell)cell).getValueByteBuffer(),((ByteBufferedCell)cell).getValuePositionInByteBuffer());
  }
  return Bytes.toDouble(cell.getValueArray(),cell.getValueOffset());
}
