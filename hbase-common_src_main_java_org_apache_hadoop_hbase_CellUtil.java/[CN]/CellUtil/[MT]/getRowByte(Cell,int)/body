{
  if (cell instanceof ByteBufferedCell) {
    return ((ByteBufferedCell)cell).getRowByteBuffer().get(((ByteBufferedCell)cell).getRowPositionInByteBuffer() + index);
  }
  return cell.getRowArray()[cell.getRowOffset() + index];
}
