{
  if (this.equals(request)) {
    return 0;
  }
  int compareVal;
  compareVal=p - request.p;
  if (compareVal != 0) {
    return compareVal;
  }
  compareVal=date.compareTo(request.date);
  if (compareVal != 0) {
    return compareVal;
  }
  return -1;
}
