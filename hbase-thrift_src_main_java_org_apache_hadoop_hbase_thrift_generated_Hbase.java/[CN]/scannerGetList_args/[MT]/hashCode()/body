{
  List<Object> list=new ArrayList<Object>();
  boolean present_id=true;
  list.add(present_id);
  if (present_id)   list.add(id);
  boolean present_nbRows=true;
  list.add(present_nbRows);
  if (present_nbRows)   list.add(nbRows);
  return list.hashCode();
}
