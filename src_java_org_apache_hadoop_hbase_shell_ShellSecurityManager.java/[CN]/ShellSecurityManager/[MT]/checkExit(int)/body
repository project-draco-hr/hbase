{
  if (status != 9999) {
    Shell shell=new Shell();
    String[] args=new String[]{String.valueOf(status)};
    try {
      shell.main(args);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
}
