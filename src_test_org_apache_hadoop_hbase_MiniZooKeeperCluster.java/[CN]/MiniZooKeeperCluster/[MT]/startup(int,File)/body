{
  setupTestEnv();
  shutdown();
  if (numPeers < 1) {
    return;
  }
  this.numPeers=numPeers;
  this.baseDir=baseDir.getAbsoluteFile();
  if (isDistributed()) {
    startupDistributed();
  }
 else {
    startupStandalone();
  }
  started=true;
}
