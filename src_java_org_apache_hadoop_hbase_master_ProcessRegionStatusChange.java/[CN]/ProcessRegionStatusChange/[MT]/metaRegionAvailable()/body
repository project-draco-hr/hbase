{
  boolean available=true;
  if (isMetaTable) {
    if (!rootAvailable()) {
      requeue();
      available=false;
    }
  }
 else {
    if (!master.regionManager.isInitialRootScanComplete() || !metaTableAvailable()) {
      requeue();
      available=false;
    }
  }
  return available;
}
