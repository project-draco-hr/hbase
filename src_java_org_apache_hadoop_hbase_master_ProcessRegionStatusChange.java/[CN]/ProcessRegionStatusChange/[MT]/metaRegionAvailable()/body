{
  boolean available=true;
  if (isMetaTable) {
    if (!rootAvailable()) {
      available=false;
    }
  }
 else {
    if (!master.regionManager.isInitialRootScanComplete() || !metaTableAvailable()) {
      requeue();
      available=false;
    }
  }
  return available;
}
