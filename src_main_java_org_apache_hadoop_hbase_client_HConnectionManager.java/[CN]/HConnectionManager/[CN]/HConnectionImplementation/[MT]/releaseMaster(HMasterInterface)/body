{
  if (master == null) {
    return;
  }
synchronized (masterAndZKLock) {
    --keepAliveMasterUserCount;
    if (keepAliveMasterUserCount <= 0) {
      keepMasterAliveUntil=System.currentTimeMillis() + keepAlive;
    }
  }
}
