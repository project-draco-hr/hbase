{
  if (zooKeeper == null) {
    try {
      zooKeeper=new ZooKeeperWatcher(conf,ZKUtil.getZooKeeperClusterKey(conf),this);
    }
 catch (    IOException e) {
      throw new ZooKeeperConnectionException(e);
    }
  }
  return zooKeeper;
}
