{
  super.tearDown();
  if (dfsCluster != null) {
    dfsCluster.shutdown();
  }
  if (this.fs != null) {
    try {
      this.fs.close();
    }
 catch (    IOException e) {
      LOG.info("During tear down got a " + e.getMessage());
    }
  }
}
