{
  HTableDescriptor.isLegalTableName(tableName);
  return execute(new MasterCallable<Pair<Integer,Integer>>(){
    @Override public Pair<Integer,Integer> call() throws IOException {
      return master.getAlterStatus(tableName);
    }
  }
);
}
