{
  this.dataBlockEncoder=dataBlockEncoder;
  uncompressedOutputStream=new ByteArrayOutputStream(BUFFER_SIZE);
  encodingCxt=dataBlockEncoder.newDataBlockEncodingContext(Compression.Algorithm.NONE,encoding,HFileBlock.DUMMY_HEADER);
}
