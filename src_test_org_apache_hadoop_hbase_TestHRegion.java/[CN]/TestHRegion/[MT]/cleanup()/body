{
  try {
    log.closeAndDelete();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  if (cluster != null) {
    cluster.shutdown();
    cluster=null;
  }
  deleteFile(new File(System.getProperty("test.build.data"),"dfs"));
}
