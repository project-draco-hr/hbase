{
  try {
    r.close();
    r=null;
    log.closeAndDelete();
    log=null;
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  deleteFile(new File(System.getProperty("test.build.data"),"dfs"));
}
