{
  try {
    setup();
    locks();
    badPuts();
    basic();
    scan();
    batchWrite();
    splitAndMerge();
    read();
    cleanup();
  }
  finally {
    if (cluster != null) {
      cluster.shutdown();
    }
  }
}
