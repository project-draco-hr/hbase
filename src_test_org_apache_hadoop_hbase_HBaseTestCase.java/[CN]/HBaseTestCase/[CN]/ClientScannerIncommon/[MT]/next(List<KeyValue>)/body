{
  RowResult results=scanner.next();
  if (results == null) {
    return false;
  }
  values.clear();
  for (  Map.Entry<byte[],Cell> entry : results.entrySet()) {
    values.add(new KeyValue(results.getRow(),entry.getKey(),entry.getValue().getTimestamp(),entry.getValue().getValue()));
  }
  return true;
}
