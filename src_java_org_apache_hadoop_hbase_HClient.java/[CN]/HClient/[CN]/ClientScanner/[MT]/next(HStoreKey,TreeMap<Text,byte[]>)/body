{
  if (closed) {
    return false;
  }
  boolean status=scanner.next(key,results);
  if (!status) {
    status=nextScanner();
    if (status) {
      status=scanner.next(key,results);
    }
  }
  return status;
}
