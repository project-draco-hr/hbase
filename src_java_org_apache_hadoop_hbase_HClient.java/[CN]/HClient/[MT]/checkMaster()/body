{
  if (this.master != null) {
    return;
  }
  HServerAddress masterLocation=new HServerAddress(this.conf.get(MASTER_ADDRESS));
  this.master=(HMasterInterface)RPC.getProxy(HMasterInterface.class,HMasterInterface.versionID,masterLocation.getInetSocketAddress(),this.conf);
}
