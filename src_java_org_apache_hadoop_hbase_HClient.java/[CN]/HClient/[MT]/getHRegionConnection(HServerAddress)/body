{
  HRegionInterface server=servers.get(regionServer.toString());
  if (server == null) {
    server=(HRegionInterface)RPC.waitForProxy(HRegionInterface.class,HRegionInterface.versionID,regionServer.getInetSocketAddress(),conf);
    servers.put(regionServer.toString(),server);
  }
  return server;
}
