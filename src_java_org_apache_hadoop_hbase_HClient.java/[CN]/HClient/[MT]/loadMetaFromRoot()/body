{
  SortedMap<Text,TableInfo> rootRegion=this.tablesToServers.get(ROOT_TABLE_NAME);
  if (rootRegion == null) {
    rootRegion=locateRootRegion();
  }
  return scanRoot(rootRegion.get(rootRegion.firstKey()));
}
