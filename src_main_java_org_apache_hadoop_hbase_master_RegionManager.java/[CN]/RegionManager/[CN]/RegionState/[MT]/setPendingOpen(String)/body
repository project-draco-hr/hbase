{
  if (state != State.UNASSIGNED) {
    LOG.warn("Cannot assign a region that is not currently unassigned. " + "FIX!! State: " + toString());
  }
  state=State.PENDING_OPEN;
  this.serverName=serverName;
}
