{
  super(2,false);
  conf.set(HConstants.REGION_SERVER_CLASS,TransactionalRegionInterface.class.getName());
  conf.set(HConstants.REGION_SERVER_IMPL,TransactionalRegionServer.class.getName());
  conf.setInt("ipc.client.connect.max.retries",5);
  conf.setInt("ipc.client.timeout",10000);
  conf.setInt("hbase.client.pause",10000);
  conf.setInt("hbase.client.retries.number",10);
}
