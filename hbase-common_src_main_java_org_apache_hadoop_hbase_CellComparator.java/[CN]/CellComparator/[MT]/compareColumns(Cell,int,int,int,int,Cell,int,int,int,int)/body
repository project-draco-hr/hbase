{
  int diff=compareFamilies(left.getFamilyArray(),lfamilyOffset,lfamilylength,right.getFamilyArray(),rfamilyOffset,rfamilylength);
  if (diff != 0) {
    return diff;
  }
 else {
    return compareQualifiers(left.getQualifierArray(),lqualOffset,lQualLength,right.getQualifierArray(),rqualOffset,rqualLength);
  }
}
