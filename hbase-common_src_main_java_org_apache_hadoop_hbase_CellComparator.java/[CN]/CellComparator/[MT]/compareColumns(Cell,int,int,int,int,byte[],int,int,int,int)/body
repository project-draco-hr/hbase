{
  int diff=compareFamilies(left.getFamilyArray(),lfamilyOffset,lfamilylength,right,rfamilyOffset,rfamilylength);
  if (diff != 0) {
    return diff;
  }
 else {
    return compareQualifiers(left.getQualifierArray(),lqualOffset,lQualLength,right,rqualOffset,rqualLength);
  }
}
