{
  return " state= " + currentAlterStatus + ", ts= "+ stamp+ ", number of regions to process = "+ numberOfRegionsToProcess+ ", number of regions processed = "+ numberOfRegionsProcessed+ ", hosts = "+ hostsToProcess+ " , processed hosts = "+ processedHosts+ " , errorCause = "+ errorCause;
}
