{
  HBaseProtos.TableDescriptor.Builder builder=HBaseProtos.TableDescriptor.newBuilder().setSchema(hTableDescriptor.convert());
  if (tableState != null)   builder.setState(tableState.convert());
  return builder.build();
}
