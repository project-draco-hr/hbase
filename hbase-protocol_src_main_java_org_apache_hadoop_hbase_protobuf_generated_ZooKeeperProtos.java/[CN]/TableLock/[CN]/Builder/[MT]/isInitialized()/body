{
  if (!hasTableName()) {
    return false;
  }
  if (!hasLockOwner()) {
    return false;
  }
  if (!hasThreadId()) {
    return false;
  }
  if (!hasIsShared()) {
    return false;
  }
  if (!getLockOwner().isInitialized()) {
    return false;
  }
  return true;
}
