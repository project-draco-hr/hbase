{
  long result=-1;
  DataInputStream dis=null;
  try {
    dis=new DataInputStream(new ByteArrayInputStream(bytes));
    result=dis.readLong();
  }
  finally {
    dis.close();
  }
  return result;
}
