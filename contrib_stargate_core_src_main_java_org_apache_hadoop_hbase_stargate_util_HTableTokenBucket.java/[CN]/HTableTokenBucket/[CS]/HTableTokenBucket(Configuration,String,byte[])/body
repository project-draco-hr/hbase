{
  this.conf=conf;
  this.tableName=tableName;
  this.row=row;
  this.table=new HTable(conf,tableName);
  this.configUpdateInterval=conf.getLong("stargate.tb.update.interval",1000 * 60);
  updateConfig();
}
