{
  if (currentWriter == null && existingWriters.isEmpty()) {
    sanityCheckLeft(this.boundaries.get(0),kv.getRowArray(),kv.getRowOffset(),kv.getRowLength());
  }
  prepareWriterFor(kv);
  currentWriter.append(kv);
  lastKv=kv;
  ++kvsInCurrentWriter;
}
