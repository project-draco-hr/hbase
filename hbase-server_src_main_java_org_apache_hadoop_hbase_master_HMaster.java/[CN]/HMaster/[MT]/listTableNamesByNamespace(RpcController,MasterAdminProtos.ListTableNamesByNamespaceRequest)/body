{
  try {
    MasterAdminProtos.ListTableNamesByNamespaceResponse.Builder b=MasterAdminProtos.ListTableNamesByNamespaceResponse.newBuilder();
    for (    TableName tableName : listTableNamesByNamespace(request.getNamespaceName())) {
      b.addTableName(ProtobufUtil.toProtoTableName(tableName));
    }
    return b.build();
  }
 catch (  IOException e) {
    throw new ServiceException(e);
  }
}
