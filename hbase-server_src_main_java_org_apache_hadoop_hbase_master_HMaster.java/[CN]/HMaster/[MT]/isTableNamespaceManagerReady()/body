{
  boolean ready=tableNamespaceManager != null && tableNamespaceManager.isTableAvailableAndInitialized();
  if (!ready) {
    LOG.warn("Table Namespace Manager not ready yet");
  }
  return ready;
}
