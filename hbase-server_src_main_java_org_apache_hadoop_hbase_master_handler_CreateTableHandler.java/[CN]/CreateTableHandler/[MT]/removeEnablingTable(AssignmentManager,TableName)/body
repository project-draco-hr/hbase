{
  try {
    assignmentManager.getTableStateManager().checkAndRemoveTableState(tableName,ZooKeeperProtos.Table.State.ENABLING,false);
  }
 catch (  CoordinatedStateException e) {
    LOG.error("Got a keeper exception while removing the ENABLING table znode " + tableName,e);
  }
}
