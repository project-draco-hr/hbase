{
  List<HRegionInfo> regions=ModifyRegionUtils.createRegions(conf,fileSystemManager.getRootDir(),hTableDescriptor,newRegions,catalogTracker);
  if (regions != null && regions.size() > 0) {
    MetaEditor.addRegionsToMeta(catalogTracker,regions);
  }
  return regions;
}
