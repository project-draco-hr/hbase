{
  try {
    HTable table=getTable(tableName);
    Scanner scanner=table.getScanner(columns.toArray(new byte[0][]),startRow);
    return addScanner(scanner);
  }
 catch (  IOException e) {
    throw new IOError(e.getMessage());
  }
}
