{
  try {
    HTable table=getTable(tableName);
    Cell[] cells=table.get(row,column,numVersions);
    return ThriftUtilities.cellFromHBase(cells);
  }
 catch (  IOException e) {
    throw new IOError(e.getMessage());
  }
}
