{
  if (this.getRegionInfo().isMetaRegion()) {
    this.log.sync(txid);
  }
 else {
switch (durability) {
case USE_DEFAULT:
      if (shouldSyncLog()) {
        this.log.sync(txid);
      }
    break;
case SKIP_WAL:
  break;
case ASYNC_WAL:
break;
case SYNC_WAL:
case FSYNC_WAL:
this.log.sync(txid);
break;
}
}
}
