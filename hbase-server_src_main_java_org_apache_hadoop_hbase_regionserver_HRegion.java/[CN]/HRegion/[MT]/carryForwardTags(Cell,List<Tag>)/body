{
  if (cell.getTagsLength() <= 0)   return tags;
  List<Tag> newTags=tags == null ? new ArrayList<Tag>() : tags;
  Iterator<Tag> i=CellUtil.tagsIterator(cell);
  while (i.hasNext())   newTags.add(i.next());
  return newTags;
}
