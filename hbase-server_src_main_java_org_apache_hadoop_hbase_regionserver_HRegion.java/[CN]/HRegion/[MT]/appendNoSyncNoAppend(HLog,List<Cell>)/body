{
  HLogKey key=new HLogKey(getRegionInfo().getEncodedNameAsBytes(),getRegionInfo().getTable(),HLog.NO_SEQUENCE_ID,0,null,HConstants.NO_NONCE,HConstants.NO_NONCE);
  wal.appendNoSync(getTableDesc(),getRegionInfo(),key,WALEdit.EMPTY_WALEDIT,this.sequenceId,false,cells);
  return key;
}
