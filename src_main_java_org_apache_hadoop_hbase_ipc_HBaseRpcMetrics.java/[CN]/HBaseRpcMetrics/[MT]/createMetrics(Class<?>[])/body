{
  for (  Class<?> iface : ifaces) {
    Method[] methods=iface.getMethods();
    for (    Method method : methods) {
      if (get(method.getName()) == null)       create(method.getName());
    }
  }
}
