{
  String position="N/A";
  try {
    if (this.reader != null) {
      position=this.reader.getPosition() + "";
    }
  }
 catch (  IOException ioe) {
  }
  return "Total replicated edits: " + totalReplicatedEdits + ", currently replicating from: "+ this.currentPath+ " at position: "+ position;
}
