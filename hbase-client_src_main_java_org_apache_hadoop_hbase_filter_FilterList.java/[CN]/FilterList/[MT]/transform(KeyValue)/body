{
  if (!v.equals(this.referenceCell)) {
    throw new IllegalStateException("Reference Cell: " + this.referenceCell + " does not match: "+ v);
  }
  return KeyValueUtil.ensureKeyValue(this.transformedCell);
}
