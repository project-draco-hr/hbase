{
  if (!env.getMasterServices().isInitialized()) {
    if (nsDescriptor.equals(NamespaceDescriptor.DEFAULT_NAMESPACE) || nsDescriptor.equals(NamespaceDescriptor.SYSTEM_NAMESPACE)) {
      return true;
    }
    return false;
  }
  return getTableNamespaceManager(env).acquireExclusiveLock();
}
