{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_labels=true && (isSetLabels());
  builder.append(present_labels);
  if (present_labels)   builder.append(labels);
  return builder.toHashCode();
}
