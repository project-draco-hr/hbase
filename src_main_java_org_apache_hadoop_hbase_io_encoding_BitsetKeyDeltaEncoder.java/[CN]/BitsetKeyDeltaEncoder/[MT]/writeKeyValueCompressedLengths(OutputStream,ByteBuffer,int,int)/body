{
  int off=in.position() - KeyValue.ROW_OFFSET;
  ByteBufferUtils.copyToStream(out,in,off + (4 - keyLengthFitsInBytes),keyLengthFitsInBytes);
  off+=KeyValue.KEY_LENGTH_SIZE;
  ByteBufferUtils.copyToStream(out,in,off + (4 - valueLengthFitsInBytes),valueLengthFitsInBytes);
}
