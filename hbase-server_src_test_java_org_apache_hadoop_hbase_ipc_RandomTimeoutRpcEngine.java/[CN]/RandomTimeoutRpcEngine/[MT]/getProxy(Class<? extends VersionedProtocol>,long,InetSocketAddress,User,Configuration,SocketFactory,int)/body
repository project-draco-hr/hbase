{
  VersionedProtocol actualProxy=super.getProxy(protocol,clientVersion,addr,ticket,conf,factory,rpcTimeout);
  RandomTimeoutInvocationHandler invoker=new RandomTimeoutInvocationHandler(actualProxy);
  VersionedProtocol object=(VersionedProtocol)Proxy.newProxyInstance(protocol.getClassLoader(),new Class[]{protocol},invoker);
  return object;
}
