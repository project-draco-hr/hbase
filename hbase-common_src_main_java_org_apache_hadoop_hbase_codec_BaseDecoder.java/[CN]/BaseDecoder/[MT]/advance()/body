{
  int firstByte=in.read();
  if (firstByte == -1) {
    return false;
  }
 else {
    in.unread(firstByte);
  }
  try {
    this.current=parseCell();
  }
 catch (  IOException ioEx) {
    in.resetBuf(1);
    rethrowEofException(ioEx);
  }
  return true;
}
