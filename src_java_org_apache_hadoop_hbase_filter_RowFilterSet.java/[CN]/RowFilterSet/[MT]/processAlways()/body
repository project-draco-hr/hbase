{
  for (  RowFilterInterface filter : filters) {
    if (filter.processAlways()) {
      if (LOG.isDebugEnabled()) {
        LOG.debug("processAlways() is true due to subfilter of type " + filter.getClass().getSimpleName());
      }
      return true;
    }
  }
  return false;
}
