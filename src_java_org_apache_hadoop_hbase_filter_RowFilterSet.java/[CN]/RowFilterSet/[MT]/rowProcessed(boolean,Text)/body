{
  for (  RowFilterInterface filter : filters) {
    filter.rowProcessed(filtered,rowKey);
    if (LOG.isDebugEnabled()) {
      LOG.debug("Called rowProcessed on subfilter of type " + filter.getClass().getSimpleName());
    }
  }
}
