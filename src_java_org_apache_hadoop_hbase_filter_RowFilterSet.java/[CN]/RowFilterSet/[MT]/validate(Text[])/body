{
  for (  RowFilterInterface filter : filters) {
    filter.validate(columns);
    if (LOG.isDebugEnabled()) {
      LOG.debug("Validated subfilter of type " + filter.getClass().getSimpleName());
    }
  }
}
