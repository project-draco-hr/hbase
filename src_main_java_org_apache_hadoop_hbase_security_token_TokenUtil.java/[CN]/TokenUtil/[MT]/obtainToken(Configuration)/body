{
  HTable meta=null;
  try {
    meta=new HTable(conf,".META.");
    AuthenticationProtocol prot=meta.coprocessorProxy(AuthenticationProtocol.class,HConstants.EMPTY_START_ROW);
    return prot.getAuthenticationToken();
  }
  finally {
    if (meta != null) {
      meta.close();
    }
  }
}
