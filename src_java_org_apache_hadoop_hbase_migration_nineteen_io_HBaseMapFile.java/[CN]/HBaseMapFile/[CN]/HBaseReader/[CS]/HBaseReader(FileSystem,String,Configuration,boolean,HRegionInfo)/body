{
  super(fs,dirName,new org.apache.hadoop.hbase.migration.nineteen.HStoreKey.HStoreKeyWritableComparator(hri),conf,false);
  this.blockCacheEnabled=blockCacheEnabled;
  open(fs,dirName,new org.apache.hadoop.hbase.migration.nineteen.HStoreKey.HStoreKeyWritableComparator(hri),conf);
}
