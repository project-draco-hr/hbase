{
  if (!hasRegion()) {
    return false;
  }
  if (!getRegion().isInitialized()) {
    return false;
  }
  for (int i=0; i < getGetCount(); i++) {
    if (!getGet(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
