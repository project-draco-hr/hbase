{
  if (!hasRegion()) {
    return false;
  }
  if (!getRegion().isInitialized()) {
    return false;
  }
  for (int i=0; i < getMutationCount(); i++) {
    if (!getMutation(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
