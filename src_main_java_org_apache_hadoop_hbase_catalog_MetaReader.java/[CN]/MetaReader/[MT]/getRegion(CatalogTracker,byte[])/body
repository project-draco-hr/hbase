{
  Get get=new Get(regionName);
  get.addFamily(HConstants.CATALOG_FAMILY);
  Result r=catalogTracker.waitForMetaServerConnectionDefault().get(CatalogTracker.META_REGION,get);
  if (r == null || r.isEmpty()) {
    return null;
  }
  return metaRowToRegionPair(r);
}
