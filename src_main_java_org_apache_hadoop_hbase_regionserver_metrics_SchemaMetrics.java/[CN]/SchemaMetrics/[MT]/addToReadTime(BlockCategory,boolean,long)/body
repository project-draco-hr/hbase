{
  RegionMetricsStorage.incrTimeVaryingMetric(getBlockMetricName(blockCategory,isCompaction,BlockMetricType.READ_TIME),timeMs);
  if (blockCategory != BlockCategory.ALL_CATEGORIES) {
    addToReadTime(BlockCategory.ALL_CATEGORIES,isCompaction,timeMs);
  }
}
