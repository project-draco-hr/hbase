{
  watcher.getZooKeeper().create(ZKSplitLog.getRescanNode(watcher),TaskState.TASK_UNASSIGNED.get(serverName),Ids.OPEN_ACL_UNSAFE,CreateMode.PERSISTENT_SEQUENTIAL,new CreateRescanAsyncCallback(),new Long(retries));
}
