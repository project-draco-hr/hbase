{
  super.splitLogAfterStartup(mfs);
  logSplit=true;
  if (getConfiguration().getBoolean("TestingMaster.sleep",false)) {
    int duration=getConfiguration().getInt("TestingMaster.sleep.duration",0);
    Threads.sleep(duration);
  }
}
