{
  HBaseClient client=clients.get(factory);
  if (client == null) {
    client=new HBaseClient(conf,factory);
    clients.put(factory,client);
  }
 else {
    client.incCount();
  }
  return client;
}
