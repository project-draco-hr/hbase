{
  byte[] encodedName=region.getEncodedNameAsBytes();
  if (this.regionsInTransitionInRS.containsKey(encodedName)) {
    boolean openAction=this.regionsInTransitionInRS.get(encodedName);
    throw new RegionAlreadyInTransitionException("Received:" + currentAction + " for the region:"+ region.getRegionNameAsString()+ " ,which we are already trying to "+ (openAction ? OPEN : CLOSE)+ ".");
  }
}
