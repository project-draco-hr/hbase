{
  boolean openAction=this.regionsInTransitionInRS.get(encodedName);
  return REGIONSERVER + ":" + this.getServerName()+ RECEIVED+ receivedAction+ ALREADY_TRANSITIONING+ (openAction ? OPEN : CLOSE)+ "; "+ region.getRegionNameAsString();
}
