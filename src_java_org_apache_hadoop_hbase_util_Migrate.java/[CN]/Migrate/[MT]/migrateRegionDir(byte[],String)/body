{
  Path tableDir=new Path(rootdir,tableName.toString());
  fs.mkdirs(tableDir);
  Path newPath=new Path(tableDir,oldPath.substring(OLD_PREFIX.length()));
  fs.rename(new Path(rootdir,oldPath),newPath);
  processRegionSubDirs(fs,newPath);
}
