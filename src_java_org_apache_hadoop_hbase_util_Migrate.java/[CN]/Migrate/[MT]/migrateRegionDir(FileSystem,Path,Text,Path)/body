{
  Path tableDir=new Path(rootdir,tableName.toString());
  fs.mkdirs(tableDir);
  Path newPath=new Path(tableDir,oldPath.getName().substring(OLD_PREFIX.length()));
  fs.rename(oldPath,newPath);
  processRegionSubDirs(fs,newPath);
}
