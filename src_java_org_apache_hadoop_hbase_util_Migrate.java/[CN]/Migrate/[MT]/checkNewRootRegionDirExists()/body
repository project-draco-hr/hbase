{
  Path rootRegionDir=HRegion.getRegionDir(FSUtils.getRootDir(this.conf),HRegionInfo.ROOT_REGIONINFO);
  boolean newRootRegion=fs.exists(rootRegionDir);
  this.migrationNeeded=!newRootRegion;
  return newRootRegion;
}
