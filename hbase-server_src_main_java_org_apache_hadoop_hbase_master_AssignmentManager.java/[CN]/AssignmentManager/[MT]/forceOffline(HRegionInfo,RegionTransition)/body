{
  LOG.debug("RIT " + hri.getEncodedName() + " in state="+ oldRt.getEventType()+ " was on deadserver; forcing offline");
  ZKAssign.createOrForceNodeOffline(this.watcher,hri,this.server.getServerName());
  addToRITandCallClose(hri,RegionState.State.OFFLINE,oldRt);
}
