{
  if (path.equals(watcher.assignmentZNode)) {
    try {
      ZKUtil.listChildrenAndWatchThem(watcher,watcher.assignmentZNode);
    }
 catch (    KeeperException e) {
      master.abort("Unexpected ZK exception reading unassigned children",e);
    }
  }
}
