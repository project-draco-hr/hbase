{
  Set<TableName> disablingTables=tableStateManager.getTablesInStates(TableState.State.DISABLING);
  if (disablingTables.size() != 0) {
    for (    TableName tableName : disablingTables) {
      LOG.info("The table " + tableName + " is in DISABLING state.  Hence recovering by moving the table"+ " to DISABLED state.");
      new DisableTableHandler(this.server,tableName,this,tableLockManager,true).prepare().process();
    }
  }
}
