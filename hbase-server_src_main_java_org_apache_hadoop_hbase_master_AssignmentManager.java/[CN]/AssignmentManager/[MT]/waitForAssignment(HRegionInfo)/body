{
  while (!regionStates.isRegionAssigned(regionInfo)) {
    if (regionStates.isRegionFailedToOpen(regionInfo) || this.server.isStopped()) {
      return false;
    }
    regionStates.waitForUpdate(100);
  }
  return true;
}
