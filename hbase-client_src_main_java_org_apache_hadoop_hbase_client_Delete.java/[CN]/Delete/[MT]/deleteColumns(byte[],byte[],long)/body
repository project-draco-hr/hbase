{
  List<? extends Cell> list=familyMap.get(family);
  if (list == null) {
    list=new ArrayList<Cell>();
  }
  ((List<KeyValue>)list).add(new KeyValue(this.row,family,qualifier,timestamp,KeyValue.Type.DeleteColumn));
  familyMap.put(family,list);
  return this;
}
