{
  this(columnName == null || columnName.length() <= 0 ? new Text() : new Text(columnName),DEFAULT_N_VERSIONS,DEFAULT_COMPRESSION_TYPE,DEFAULT_IN_MEMORY,Integer.MAX_VALUE,DEFAULT_BLOOM_FILTER_DESCRIPTOR);
}
