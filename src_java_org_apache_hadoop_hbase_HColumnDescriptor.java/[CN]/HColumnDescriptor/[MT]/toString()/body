{
  return "{" + NAME + " => '"+ Bytes.toString(name)+ "', "+ MAX_VERSIONS+ " => "+ maxVersions+ ", "+ COMPRESSION+ " => '"+ this.compressionType+ "', "+ IN_MEMORY+ " => "+ inMemory+ ", "+ BLOCKCACHE+ " => "+ blockCacheEnabled+ ", "+ MAX_LENGTH+ " => "+ maxValueLength+ ", "+ TTL+ " => "+ (timeToLive == HConstants.FOREVER ? "FOREVER" : Integer.toString(timeToLive))+ ", "+ BLOOMFILTER+ " => "+ (bloomFilterSpecified ? bloomFilter.toString() : CompressionType.NONE)+ "}";
}
