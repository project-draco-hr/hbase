{
  if (this.maxValueLength == null) {
    String value=getValue(LENGTH);
    this.maxValueLength=(value != null) ? Integer.decode(value) : DEFAULT_LENGTH_INTEGER;
  }
  return this.maxValueLength.intValue();
}
