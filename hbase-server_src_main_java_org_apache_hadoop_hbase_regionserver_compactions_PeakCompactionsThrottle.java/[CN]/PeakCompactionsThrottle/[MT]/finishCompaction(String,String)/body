{
  if (sleepNumber > 0) {
    LOG.info("Region '" + region + "' family '"+ family+ "' 's maxSpeedInPeak is "+ StringUtils.humanReadableInt(maxSpeedInPeak)+ "/s compaction throttle: sleep number  "+ sleepNumber+ " sleep time "+ sleepTimeTotal+ "(ms)");
  }
}
