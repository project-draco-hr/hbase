{
  StringBuilder sb=new StringBuilder();
  for (  String item : list) {
    sb.append(item);
    sb.append(COMMA);
  }
  if (sb.length() > 0) {
    sb.deleteCharAt(sb.length() - 1);
  }
  return sb.toString();
}
