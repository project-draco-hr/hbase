{
  Configuration conf=server.getConfiguration();
  noPersistence=ConfigUtil.useZKForAssignment(conf) && !conf.getBoolean("hbase.assignment.usezk.migrating",false);
  this.server=server;
  initialized=false;
}
