{
  if (!ZK_WRAPPERS.containsKey(ZooKeeperWrapper.getZookeeperClusterKey(conf))) {
    ZK_WRAPPERS.put(ZooKeeperWrapper.getZookeeperClusterKey(conf),new ClientZKWatcher(conf));
  }
  return ZK_WRAPPERS.get(ZooKeeperWrapper.getZookeeperClusterKey(conf));
}
