{
  return new StripeMultiFileWriter.WriterFactory(){
    @Override public Writer createWriter() throws IOException {
      StoreFile.Writer writer=store.createWriterInTmp(kvCount,store.getFamily().getCompressionType(),false,true,true,false);
      writer.setTimeRangeTracker(tracker);
      return writer;
    }
  }
;
}
