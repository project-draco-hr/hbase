{
  this.name=name;
  this.admin=admin;
  byte[] digest=MessageDigest.getInstance("MD5").digest(Bytes.toBytes(name));
  StringBuffer sb=new StringBuffer();
  for (int i=0; i < digest.length; i++) {
    sb.append(Integer.toHexString(0xff & digest[i]));
  }
  this.token=sb.toString();
}
