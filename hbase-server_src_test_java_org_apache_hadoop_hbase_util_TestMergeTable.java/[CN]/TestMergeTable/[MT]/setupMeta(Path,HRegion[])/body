{
  HRegion meta=HRegion.createHRegion(HRegionInfo.FIRST_META_REGIONINFO,rootdir,UTIL.getConfiguration(),HTableDescriptor.META_TABLEDESC);
  for (  HRegion r : regions) {
    HRegion.addRegionToMETA(meta,r);
  }
  HRegion.closeHRegion(meta);
}
