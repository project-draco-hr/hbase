{
  HTableDescriptor htd=new HTableDescriptor("mytable");
  List<HRegionInfo> regions=Lists.newArrayList(new HRegionInfo(htd,Bytes.toBytes("a"),Bytes.toBytes("d")),new HRegionInfo(htd,Bytes.toBytes("d"),Bytes.toBytes("z")));
  Mockito.doReturn(regions).when(rs).getOnlineRegions();
  new RSStatusTmpl().render(new StringWriter(),rs);
}
