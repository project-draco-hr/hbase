{
  rs.doMetrics();
  RegionServerMetrics metrics=rs.getMetrics();
  assertEquals("checkAndMutatePassed metrics incorrect",expectedPassed,metrics.checkAndMutateChecksPassed.get());
  assertEquals("checkAndMutateFailed metrics incorrect",expectedFailed,metrics.checkAndMutateChecksFailed.get());
}
