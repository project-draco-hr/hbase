{
  this.name=name;
  threadPoolExecutor=new ThreadPoolExecutor(corePoolSize,maximumPoolSize,keepAliveTimeInMillis,TimeUnit.MILLISECONDS,workQueue);
  threadPoolExecutor.setThreadFactory(new NamedThreadFactory(name));
}
