{
  HRegionInfo[] infos=new HRegionInfo[]{newInfo1,newInfo2};
  for (  HRegionInfo info : infos) {
    add(HistorianColumnKey.REGION_SPLIT.key,SPLIT_PREFIX + oldInfo.getRegionNameAsString(),info);
  }
}
