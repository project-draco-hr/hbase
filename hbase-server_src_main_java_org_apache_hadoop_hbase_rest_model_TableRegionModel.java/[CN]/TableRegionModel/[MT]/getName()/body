{
  byte[] tableNameAsBytes=Bytes.toBytes(this.table);
  byte[] nameAsBytes=HRegionInfo.createRegionName(tableNameAsBytes,this.startKey,this.id,!HTableDescriptor.isMetaTable(tableNameAsBytes));
  return Bytes.toString(nameAsBytes);
}
