{
  checkOpen();
  super.getRequestCount().incrementAndGet();
  try {
    return getTransactionalRegion(regionName).get(transactionId,row,column,timestamp,numVersions);
  }
 catch (  IOException e) {
    checkFileSystem();
    throw e;
  }
}
