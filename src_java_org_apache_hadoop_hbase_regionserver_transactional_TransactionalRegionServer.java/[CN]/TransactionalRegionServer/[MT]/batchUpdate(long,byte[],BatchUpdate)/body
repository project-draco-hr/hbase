{
  checkOpen();
  super.getRequestCount().incrementAndGet();
  try {
    getTransactionalRegion(regionName).batchUpdate(transactionId,b);
  }
 catch (  IOException e) {
    checkFileSystem();
    throw e;
  }
}
