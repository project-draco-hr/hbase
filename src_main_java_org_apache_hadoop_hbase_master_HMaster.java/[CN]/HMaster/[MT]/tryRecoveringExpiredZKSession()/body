{
  this.zooKeeper=new ZooKeeperWatcher(conf,MASTER + ":" + this.serverName.getPort(),this);
  if (!becomeActiveMaster()) {
    return false;
  }
  initializeZKBasedSystemTrackers();
  assignRootAndMeta();
  this.assignmentManager.processRegionsInTransition();
  return true;
}
