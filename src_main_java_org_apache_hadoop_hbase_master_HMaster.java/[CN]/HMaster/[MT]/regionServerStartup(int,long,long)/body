{
  this.serverManager.regionServerStartup(HBaseServer.getRemoteIp(),port,serverStartCode,serverCurrentTime);
  MapWritable mw=createConfigurationSubset();
  mw.put(new Text(HConstants.KEY_FOR_HOSTNAME_SEEN_BY_MASTER),new Text(this.serverName.getHostname()));
  return mw;
}
