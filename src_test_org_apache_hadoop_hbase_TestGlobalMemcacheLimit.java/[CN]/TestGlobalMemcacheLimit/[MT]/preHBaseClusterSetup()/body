{
  conf.setInt("hbase.regionserver.globalMemcacheLimit",2 * 1024 * 1024);
  conf.setInt("hbase.regionserver.globalMemcacheLimitLowMark",1 * 1024 * 1024);
  conf.setInt("hbase.regionserver.optionalcacheflushinterval",120000);
}
